import { Effect, Option, Schema, pipe } from "effect"
import { RpcGroup } from "effect/unstable/rpc"
import { Command, Runtime, type Update } from "foldkit"
import { type Document, type Html, type HtmlBuilder } from "foldkit/html"
import { defineMessageUnion } from "foldkit/message"
import { evo } from "foldkit/struct"
import { dataTable, field, primaryButton, quietButton, selectInput, shell, textInput, textareaInput } from "@effect-domains/example-web/html"
import { Form } from "@effect-domains/example-web/form"
import { Page } from "@effect-domains/example-web/page"
import { Requests, RequestStateSchema, RequestTokenSchema, type RequestToken } from "@effect-domains/example-web/requests"
import { formatRpcError } from "@effect-domains/example-web/rpc"
import { RpcService, type Type } from "effect-domains/rpc-service"
import { RepairBoardRowSchema } from "../board.ts"
import { CustomerSchema, RepairJobSchema, RepairStatusSchema, TechnicianSchema } from "../domain.ts"
import { CustomersResource, RepairJobsResource, TechniciansResource } from "../resources.ts"
import { RepairWorkshopOperations } from "../sqlite.ts"

const CustomerRowSchema = CustomersResource.table.rowSchema
const TechnicianRowSchema = TechniciansResource.table.rowSchema
const CustomerPageSchema = Page.schema(CustomerRowSchema)
const TechnicianPageSchema = Page.schema(TechnicianRowSchema)
const RepairJobRowSchema = RepairJobsResource.table.rowSchema
const RepairWorkshopWebRpcs = RpcGroup.make().merge(CustomersResource.group, TechniciansResource.group, RepairJobsResource.group, RepairWorkshopOperations.group)
export const WebClient = RpcService.make({ name: "repair-workshop/WebClient", group: RepairWorkshopWebRpcs })
export type WebClient = Type<typeof WebClient>

const statuses = ["queued", "repairing", "ready"] as const
const statusChoices = statuses.map((value) => ({ value, label: value }))
const filterChoices = [{ value: "", label: "All statuses" }, ...statusChoices]
const booleanChoices = [{ value: "false", label: "No" }, { value: "true", label: "Yes" }]
export const Model = Schema.Struct({
  customers: Schema.Array(CustomerRowSchema), technicians: Schema.Array(TechnicianRowSchema), board: Schema.Array(RepairBoardRowSchema), customerCursor: Schema.NullOr(Schema.String), technicianCursor: Schema.NullOr(Schema.String), filterStatus: Schema.String,
  customerId: Schema.String, customerName: Schema.String, editingCustomerId: Schema.NullOr(Schema.String), technicianId: Schema.String, technicianName: Schema.String, technicianOnCall: Schema.Boolean, editingTechnicianId: Schema.NullOr(Schema.String),
  repairCustomerId: Schema.String, repairItem: Schema.String, repairFault: Schema.String, repairUrgent: Schema.Boolean, repairStatus: RepairStatusSchema, repairTechnicianId: Schema.String,
  requests: RequestStateSchema, fieldErrors: Schema.Record(Schema.String, Schema.String), notice: Schema.NullOr(Schema.Struct({ kind: Schema.Literals(["info", "error", "success"]), text: Schema.String })),
})
export type Model = typeof Model.Type
export const Message = defineMessageUnion({
  ChangedFilterStatus: { value: Schema.String }, ChangedCustomerId: { value: Schema.String }, ChangedCustomerName: { value: Schema.String }, ChangedTechnicianId: { value: Schema.String }, ChangedTechnicianName: { value: Schema.String }, ChangedTechnicianOnCall: { value: Schema.String }, ChangedRepairCustomer: { value: Schema.String }, ChangedRepairItem: { value: Schema.String }, ChangedRepairFault: { value: Schema.String }, ChangedRepairUrgent: { value: Schema.String }, ChangedRepairStatus: { value: Schema.String }, ChangedRepairTechnician: { value: Schema.String },
  ClickedReload: {}, ClickedNextCustomers: {}, ClickedNextTechnicians: {}, ClickedSaveCustomer: {}, ClickedEditCustomer: { id: Schema.String }, ClickedClearCustomer: {}, ClickedSaveTechnician: {}, ClickedEditTechnician: { id: Schema.String }, ClickedClearTechnician: {}, ClickedCreateRepair: {}, ChangedBoardStatus: { id: Schema.String, value: Schema.String }, ChangedBoardTechnician: { id: Schema.String, value: Schema.String },
  SucceededCustomers: { page: CustomerPageSchema, append: Schema.Boolean, request: RequestTokenSchema }, SucceededTechnicians: { page: TechnicianPageSchema, append: Schema.Boolean, request: RequestTokenSchema }, SucceededBoard: { rows: Schema.Array(RepairBoardRowSchema), request: RequestTokenSchema }, SucceededCustomer: { customer: CustomerRowSchema, created: Schema.Boolean, request: RequestTokenSchema }, SucceededTechnician: { technician: TechnicianRowSchema, created: Schema.Boolean, request: RequestTokenSchema }, SucceededRepair: { repair: RepairJobRowSchema, created: Schema.Boolean, request: RequestTokenSchema }, Failed: { request: RequestTokenSchema, error: Schema.String, field: Schema.NullOr(Schema.String) },
})
export type Message = typeof Message.Type
type UpdateReturn = Update.Return<Model, Message, WebClient>
type FormFailure = Readonly<{ _tag: "FormFailure"; field: string; error: string }>
const formFailure = (field: string) => (error: Schema.SchemaError): FormFailure => ({ _tag: "FormFailure", field, error: Form.errors(error)["$"] ?? error.message })
const isFormFailure = (error: unknown): error is FormFailure => typeof error === "object" && error !== null && "_tag" in error && error._tag === "FormFailure"
const failed = (request: RequestToken, error: unknown) => Message.Failed({ request, error: isFormFailure(error) ? error.error : formatRpcError(error), field: isFormFailure(error) ? error.field : null })
const withError = <M>(h: HtmlBuilder<M>, child: Html, error: string | undefined) => h.div([], [child, error === undefined ? h.empty : h.p([h.Class("field-error")], [error])])
const emptyCustomer = { customerId: "", customerName: "", editingCustomerId: null as string | null }
const emptyTechnician = { technicianId: "", technicianName: "", technicianOnCall: false, editingTechnicianId: null as string | null }
const emptyRepair = { repairCustomerId: "", repairItem: "", repairFault: "", repairUrgent: false, repairStatus: "queued" as const, repairTechnicianId: "" }

export const LoadCustomers = Command.define("LoadCustomers", { args: { cursor: Schema.NullOr(Schema.String), append: Schema.Boolean, request: RequestTokenSchema }, messages: [Message.SucceededCustomers, Message.Failed], execute: (args) => Effect.gen(function*() { const client = yield* WebClient; const page = yield* client["customers.list"]({ limit: 50, ...Page.input(args.cursor) }); return Message.SucceededCustomers({ page, append: args.append, request: args.request }) }).pipe(Effect.catch((error) => Effect.succeed(failed(args.request, error)))) })
export const LoadTechnicians = Command.define("LoadTechnicians", { args: { cursor: Schema.NullOr(Schema.String), append: Schema.Boolean, request: RequestTokenSchema }, messages: [Message.SucceededTechnicians, Message.Failed], execute: (args) => Effect.gen(function*() { const client = yield* WebClient; const page = yield* client["technicians.list"]({ limit: 50, ...Page.input(args.cursor) }); return Message.SucceededTechnicians({ page, append: args.append, request: args.request }) }).pipe(Effect.catch((error) => Effect.succeed(failed(args.request, error)))) })
export const LoadBoard = Command.define("LoadBoard", { args: { status: Schema.String, request: RequestTokenSchema }, messages: [Message.SucceededBoard, Message.Failed], execute: ({ status, request }) => Effect.gen(function*() { const client = yield* WebClient; const rows = yield* client["workshop.board"]({ limit: 100, ...(status === "" ? {} : { status: status as typeof RepairStatusSchema.Type }) }); return Message.SucceededBoard({ rows, request }) }).pipe(Effect.catch((error) => Effect.succeed(failed(request, error)))) })
export const SaveCustomer = Command.define("SaveCustomer", { args: { id: Schema.String, name: Schema.String, editingId: Schema.NullOr(Schema.String), request: RequestTokenSchema }, messages: [Message.SucceededCustomer, Message.Failed], execute: (args) => Effect.gen(function*() { const id = yield* Schema.decodeUnknownEffect(CustomerSchema.fields.id)(args.id.trim()).pipe(Effect.mapError(formFailure("customerId"))); const name = yield* Schema.decodeUnknownEffect(CustomerSchema.fields.name)(args.name.trim()).pipe(Effect.mapError(formFailure("customerName"))); const client = yield* WebClient; const created = args.editingId === null; const customer = yield* (created ? client["customers.create"]({ id, name }) : client["customers.patch"]({ key: args.editingId, changes: { name } })); return Message.SucceededCustomer({ customer, created, request: args.request }) }).pipe(Effect.catch((error) => Effect.succeed(failed(args.request, error)))) })
export const SaveTechnician = Command.define("SaveTechnician", { args: { id: Schema.String, name: Schema.String, onCall: Schema.Boolean, editingId: Schema.NullOr(Schema.String), request: RequestTokenSchema }, messages: [Message.SucceededTechnician, Message.Failed], execute: (args) => Effect.gen(function*() { const id = yield* Schema.decodeUnknownEffect(TechnicianSchema.fields.id)(args.id.trim()).pipe(Effect.mapError(formFailure("technicianId"))); const name = yield* Schema.decodeUnknownEffect(TechnicianSchema.fields.name)(args.name.trim()).pipe(Effect.mapError(formFailure("technicianName"))); const client = yield* WebClient; const created = args.editingId === null; const technician = yield* (created ? client["technicians.create"]({ id, name, onCall: args.onCall }) : client["technicians.patch"]({ key: args.editingId, changes: { name, onCall: args.onCall } })); return Message.SucceededTechnician({ technician, created, request: args.request }) }).pipe(Effect.catch((error) => Effect.succeed(failed(args.request, error)))) })
export const SaveRepair = Command.define("SaveRepair", { args: { customerId: Schema.String, item: Schema.String, fault: Schema.String, urgent: Schema.Boolean, status: RepairStatusSchema, technicianId: Schema.String, request: RequestTokenSchema }, messages: [Message.SucceededRepair, Message.Failed], execute: (args) => Effect.gen(function*() { const customerId = yield* Schema.decodeUnknownEffect(RepairJobSchema.fields.customerId)(args.customerId.trim()).pipe(Effect.mapError(formFailure("repairCustomerId"))); const item = yield* Schema.decodeUnknownEffect(RepairJobSchema.fields.item)(args.item.trim()).pipe(Effect.mapError(formFailure("repairItem"))); const fault = yield* Schema.decodeUnknownEffect(RepairJobSchema.fields.fault)(args.fault.trim()).pipe(Effect.mapError(formFailure("repairFault"))); const technicianId = args.technicianId === "" ? null : yield* Schema.decodeUnknownEffect(Schema.NonEmptyString)(args.technicianId).pipe(Effect.mapError(formFailure("repairTechnicianId"))); const client = yield* WebClient; const repair = yield* client["repair_jobs.create"]({ customerId, item, fault, urgent: args.urgent, status: args.status, technicianId }); return Message.SucceededRepair({ repair, created: true, request: args.request }) }).pipe(Effect.catch((error) => Effect.succeed(failed(args.request, error)))) })
export const PatchRepairStatus = Command.define("PatchRepairStatus", { args: { id: Schema.String, status: RepairStatusSchema, request: RequestTokenSchema }, messages: [Message.SucceededRepair, Message.Failed], execute: ({ id, status, request }) => Effect.gen(function*() { const client = yield* WebClient; const repair = yield* client["repair_jobs.patch"]({ key: id, changes: { status } }); return Message.SucceededRepair({ repair, created: false, request }) }).pipe(Effect.catch((error) => Effect.succeed(failed(request, error)))) })
export const PatchRepairTechnician = Command.define("PatchRepairTechnician", { args: { id: Schema.String, technicianId: Schema.NullOr(Schema.String), request: RequestTokenSchema }, messages: [Message.SucceededRepair, Message.Failed], execute: ({ id, technicianId, request }) => Effect.gen(function*() { const client = yield* WebClient; const repair = yield* client["repair_jobs.patch"]({ key: id, changes: { technicianId } }); return Message.SucceededRepair({ repair, created: false, request }) }).pipe(Effect.catch((error) => Effect.succeed(failed(request, error)))) })

const refresh = (model: Model) => {
  const customers = Requests.start(model.requests, "customers.list")
  const technicians = Requests.start(customers.state, "technicians.list")
  const board = Requests.start(technicians.state, "workshop.board")
  return {
    state: board.state,
    commands: [
      LoadCustomers({ cursor: null, append: false, request: customers.request }),
      LoadTechnicians({ cursor: null, append: false, request: technicians.request }),
      LoadBoard({ status: model.filterStatus, request: board.request }),
    ],
  }
}
const beginCustomers = (model: Model, append: boolean) => { const started = Requests.start(model.requests, "customers.list"); return { state: started.state, command: LoadCustomers({ cursor: model.customerCursor, append, request: started.request }) } }
const beginTechnicians = (model: Model, append: boolean) => { const started = Requests.start(model.requests, "technicians.list"); return { state: started.state, command: LoadTechnicians({ cursor: model.technicianCursor, append, request: started.request }) } }

export const update = (model: Model, message: Message) => Message.match<UpdateReturn>(message, {
  ChangedFilterStatus: ({ value }) => ({ model: evo(model, { filterStatus: () => value, board: () => [], requests: () => Requests.invalidate(model.requests, "workshop.board") }) }), ChangedCustomerId: ({ value }) => ({ model: evo(model, { customerId: () => value }) }), ChangedCustomerName: ({ value }) => ({ model: evo(model, { customerName: () => value }) }), ChangedTechnicianId: ({ value }) => ({ model: evo(model, { technicianId: () => value }) }), ChangedTechnicianName: ({ value }) => ({ model: evo(model, { technicianName: () => value }) }), ChangedTechnicianOnCall: ({ value }) => ({ model: evo(model, { technicianOnCall: () => value === "true" }) }), ChangedRepairCustomer: ({ value }) => ({ model: evo(model, { repairCustomerId: () => value }) }), ChangedRepairItem: ({ value }) => ({ model: evo(model, { repairItem: () => value }) }), ChangedRepairFault: ({ value }) => ({ model: evo(model, { repairFault: () => value }) }), ChangedRepairUrgent: ({ value }) => ({ model: evo(model, { repairUrgent: () => value === "true" }) }), ChangedRepairStatus: ({ value }) => ({ model: evo(model, { repairStatus: () => value as Model["repairStatus"] }) }), ChangedRepairTechnician: ({ value }) => ({ model: evo(model, { repairTechnicianId: () => value }) }),
  ClickedReload: () => { const next = refresh(model); return { model: evo(model, { customers: () => [], technicians: () => [], board: () => [], customerCursor: () => null, technicianCursor: () => null, requests: () => next.state, notice: () => null }), commands: next.commands } },
  ClickedNextCustomers: () => model.customerCursor === null || Requests.pending(model.requests, "customers.list") ? { model } : (() => { const next = beginCustomers(model, true); return { model: evo(model, { requests: () => next.state }), commands: [next.command] } })(),
  ClickedNextTechnicians: () => model.technicianCursor === null || Requests.pending(model.requests, "technicians.list") ? { model } : (() => { const next = beginTechnicians(model, true); return { model: evo(model, { requests: () => next.state }), commands: [next.command] } })(),
  ClickedSaveCustomer: () => { const started = Requests.start(model.requests, "customers.save"); return { model: evo(model, { requests: () => started.state, fieldErrors: () => ({}), notice: () => null }), commands: [SaveCustomer({ id: model.customerId, name: model.customerName, editingId: model.editingCustomerId, request: started.request })] } },
  ClickedEditCustomer: ({ id }) => { const customer = model.customers.find((item) => item.id === id); return customer === undefined ? { model } : { model: evo(model, { customerId: () => customer.id, customerName: () => customer.name, editingCustomerId: () => customer.id, fieldErrors: () => ({}), notice: () => null }) } },
  ClickedClearCustomer: () => ({ model: evo(model, { customerId: () => "", customerName: () => "", editingCustomerId: () => null, fieldErrors: () => ({}), notice: () => null }) }),
  ClickedSaveTechnician: () => { const started = Requests.start(model.requests, "technicians.save"); return { model: evo(model, { requests: () => started.state, fieldErrors: () => ({}), notice: () => null }), commands: [SaveTechnician({ id: model.technicianId, name: model.technicianName, onCall: model.technicianOnCall, editingId: model.editingTechnicianId, request: started.request })] } },
  ClickedEditTechnician: ({ id }) => { const technician = model.technicians.find((item) => item.id === id); return technician === undefined ? { model } : { model: evo(model, { technicianId: () => technician.id, technicianName: () => technician.name, technicianOnCall: () => technician.onCall, editingTechnicianId: () => technician.id, fieldErrors: () => ({}), notice: () => null }) } },
  ClickedClearTechnician: () => ({ model: evo(model, { technicianId: () => "", technicianName: () => "", technicianOnCall: () => false, editingTechnicianId: () => null, fieldErrors: () => ({}), notice: () => null }) }),
  ClickedCreateRepair: () => { const started = Requests.start(model.requests, "repair_jobs.save"); return { model: evo(model, { requests: () => started.state, fieldErrors: () => ({}), notice: () => null }), commands: [SaveRepair({ customerId: model.repairCustomerId, item: model.repairItem, fault: model.repairFault, urgent: model.repairUrgent, status: model.repairStatus, technicianId: model.repairTechnicianId, request: started.request })] } },
  ChangedBoardStatus: ({ id, value }) => { const started = Requests.start(model.requests, `repair_jobs.status.${id}`); return { model: evo(model, { requests: () => started.state, notice: () => null }), commands: [PatchRepairStatus({ id, status: value as Model["repairStatus"], request: started.request })] } },
  ChangedBoardTechnician: ({ id, value }) => { const started = Requests.start(model.requests, `repair_jobs.technician.${id}`); return { model: evo(model, { requests: () => started.state, notice: () => null }), commands: [PatchRepairTechnician({ id, technicianId: value === "" ? null : value, request: started.request })] } },
  SucceededCustomers: ({ page, append, request }) => { if (!Requests.accepts(model.requests, request)) return { model }; const received = Page.receive({ items: model.customers, nextCursor: model.customerCursor }, page, append); return { model: evo(model, { customers: () => received.items, customerCursor: () => received.nextCursor, requests: () => Requests.succeed(model.requests, request) }) } },
  SucceededTechnicians: ({ page, append, request }) => { if (!Requests.accepts(model.requests, request)) return { model }; const received = Page.receive({ items: model.technicians, nextCursor: model.technicianCursor }, page, append); return { model: evo(model, { technicians: () => received.items, technicianCursor: () => received.nextCursor, requests: () => Requests.succeed(model.requests, request) }) } },
  SucceededBoard: ({ rows, request }) => !Requests.accepts(model.requests, request) ? { model } : ({ model: evo(model, { board: () => rows, requests: () => Requests.succeed(model.requests, request) }) }),
  SucceededCustomer: ({ created, request }) => { if (!Requests.accepts(model.requests, request)) return { model }; const base = evo(model, { requests: () => Requests.succeed(model.requests, request) }); const next = refresh(base); return { model: evo(model, { customerId: () => "", customerName: () => "", editingCustomerId: () => null, customers: () => [], technicians: () => [], board: () => [], customerCursor: () => null, technicianCursor: () => null, requests: () => next.state, notice: () => ({ kind: "success" as const, text: created ? "Customer created." : "Customer updated." }) }), commands: next.commands } },
  SucceededTechnician: ({ created, request }) => { if (!Requests.accepts(model.requests, request)) return { model }; const base = evo(model, { requests: () => Requests.succeed(model.requests, request) }); const next = refresh(base); return { model: evo(model, { technicianId: () => "", technicianName: () => "", technicianOnCall: () => false, editingTechnicianId: () => null, customers: () => [], technicians: () => [], board: () => [], customerCursor: () => null, technicianCursor: () => null, requests: () => next.state, notice: () => ({ kind: "success" as const, text: created ? "Technician created." : "Technician updated." }) }), commands: next.commands } },
  SucceededRepair: ({ created, request }) => { if (!Requests.accepts(model.requests, request)) return { model }; const base = evo(model, { requests: () => Requests.succeed(model.requests, request) }); const next = refresh(base); return { model: evo(model, { repairCustomerId: () => "", repairItem: () => "", repairFault: () => "", repairUrgent: () => false, repairStatus: () => "queued", repairTechnicianId: () => "", customers: () => [], technicians: () => [], board: () => [], customerCursor: () => null, technicianCursor: () => null, requests: () => next.state, notice: () => ({ kind: "success" as const, text: created ? "Repair created." : "Repair updated." }) }), commands: next.commands } },
  Failed: ({ request, error, field }) => !Requests.accepts(model.requests, request) ? { model } : ({ model: evo(model, { requests: () => Requests.fail(model.requests, request, error), fieldErrors: () => field === null ? model.fieldErrors : { ...model.fieldErrors, [field]: error }, notice: () => ({ kind: "error" as const, text: error }) }) }),
})
export const init: Runtime.ApplicationInit<Model, Message, void, WebClient> = () => { const model: Model = { customers: [], technicians: [], board: [], customerCursor: null, technicianCursor: null, filterStatus: "", ...emptyCustomer, ...emptyTechnician, ...emptyRepair, requests: Requests.empty(), fieldErrors: {}, notice: null }; const next = refresh(model); return { model: evo(model, { requests: () => next.state }), commands: next.commands } }
const technicianChoices = (technicians: ReadonlyArray<typeof TechnicianRowSchema.Type>) => [{ value: "", label: "Unassigned" }, ...technicians.map((technician) => ({ value: technician.id, label: `${technician.name} (${technician.id})` }))]
const customerChoices = (customers: ReadonlyArray<typeof CustomerRowSchema.Type>) => [{ value: "", label: "Choose a customer" }, ...customers.map((customer) => ({ value: customer.id, label: `${customer.name} (${customer.id})` }))]
const repairTechnicianChoices = (technicians: ReadonlyArray<typeof TechnicianRowSchema.Type>, repair: typeof RepairBoardRowSchema.Type) => repair.technicianId === null || technicians.some((technician) => technician.id === repair.technicianId) ? technicianChoices(technicians) : [...technicianChoices(technicians), { value: repair.technicianId, label: `${repair.technicianName} (${repair.technicianId})` }]
const onCall = (value: boolean | null) => value === null ? "—" : value ? "Yes" : "No"
export const view = (model: Model, h: HtmlBuilder<Message>): Document => ({ title: "Repair workshop", body: shell(h, { title: "Repair workshop", lede: "Create customers, technicians, and repairs. The board is a joined projection and shows up to 100 matching repairs.", notice: model.notice, session: null, children: [
  h.section([h.Class("panel stack")], [h.div([h.Class("actions")], [field(h, { id: "board-status", label: "Board status", children: selectInput(h, { id: "board-status", value: model.filterStatus, onChange: (value) => Message.ChangedFilterStatus({ value }), choices: filterChoices }) }), primaryButton(h, { label: Requests.pending(model.requests) ? "Loading…" : "Reload", message: Option.some(Message.ClickedReload()), type: "button", disabled: Requests.pending(model.requests) })]), dataTable(h, { caption: "Repair board", columns: ["Customer", "Item", "Fault", "Urgent", "Status", "Technician", "On call"], rows: model.board, key: (repair) => repair.id, cells: (repair) => [`${repair.customerName} (${repair.customerId})`, repair.item, repair.fault, repair.urgent ? "Yes" : "No", field(h, { id: `repair-status-${repair.id}`, label: "Status", children: selectInput(h, { id: `repair-status-${repair.id}`, value: repair.status, onChange: (value) => Message.ChangedBoardStatus({ id: repair.id, value }), choices: statusChoices }) }), field(h, { id: `repair-technician-${repair.id}`, label: "Technician", children: selectInput(h, { id: `repair-technician-${repair.id}`, value: repair.technicianId ?? "", onChange: (value) => Message.ChangedBoardTechnician({ id: repair.id, value }), choices: repairTechnicianChoices(model.technicians, repair) }) }), onCall(repair.technicianOnCall)] })]),
  h.div([h.Class("split")], [h.section([h.Class("panel stack")], [h.form([h.Class("stack"), h.OnSubmit(Message.ClickedCreateRepair())], [h.h2([], ["Create repair"]), field(h, { id: "repair-customer", label: "Customer", children: withError(h, selectInput(h, { id: "repair-customer", value: model.repairCustomerId, onChange: (value) => Message.ChangedRepairCustomer({ value }), choices: customerChoices(model.customers) }), model.fieldErrors.repairCustomerId) }), field(h, { id: "repair-item", label: "Item", children: withError(h, textInput(h, { id: "repair-item", value: model.repairItem, onInput: (value) => Message.ChangedRepairItem({ value }), type: "text", placeholder: "Espresso machine", autocomplete: "off" }), model.fieldErrors.repairItem) }), field(h, { id: "repair-fault", label: "Fault", children: withError(h, textareaInput(h, { id: "repair-fault", value: model.repairFault, onInput: (value) => Message.ChangedRepairFault({ value }), rows: 3 }), model.fieldErrors.repairFault) }), h.div([h.Class("actions")], [field(h, { id: "repair-urgent", label: "Urgent", children: selectInput(h, { id: "repair-urgent", value: String(model.repairUrgent), onChange: (value) => Message.ChangedRepairUrgent({ value }), choices: booleanChoices }) }), field(h, { id: "repair-new-status", label: "Status", children: selectInput(h, { id: "repair-new-status", value: model.repairStatus, onChange: (value) => Message.ChangedRepairStatus({ value }), choices: statusChoices }) }), field(h, { id: "repair-technician", label: "Technician", children: selectInput(h, { id: "repair-technician", value: model.repairTechnicianId, onChange: (value) => Message.ChangedRepairTechnician({ value }), choices: technicianChoices(model.technicians) }) })]), primaryButton(h, { label: "Create repair", message: Option.none(), type: "submit", disabled: Requests.pending(model.requests, "repair_jobs.save") })])]),
  h.section([h.Class("panel stack")], [h.form([h.Class("stack"), h.OnSubmit(Message.ClickedSaveCustomer())], [h.h2([], [model.editingCustomerId === null ? "Create customer" : "Edit customer"]), model.editingCustomerId === null ? field(h, { id: "customer-id", label: "Customer ID", children: withError(h, textInput(h, { id: "customer-id", value: model.customerId, onInput: (value) => Message.ChangedCustomerId({ value }), type: "text", placeholder: "customer-1", autocomplete: "off" }), model.fieldErrors.customerId) }) : h.p([], [`Customer ID: ${model.editingCustomerId}`]), field(h, { id: "customer-name", label: "Name", children: withError(h, textInput(h, { id: "customer-name", value: model.customerName, onInput: (value) => Message.ChangedCustomerName({ value }), type: "text", placeholder: "Ada Customer", autocomplete: "off" }), model.fieldErrors.customerName) }), h.div([h.Class("actions")], [primaryButton(h, { label: model.editingCustomerId === null ? "Create customer" : "Save customer", message: Option.none(), type: "submit", disabled: Requests.pending(model.requests, "customers.save") }), quietButton(h, { label: "Clear", message: Message.ClickedClearCustomer(), disabled: false })])]), dataTable(h, { caption: "Customers loaded for selector", columns: ["ID", "Name", ""], rows: model.customers, key: (customer) => customer.id, cells: (customer) => [customer.id, customer.name, quietButton(h, { label: "Edit", message: Message.ClickedEditCustomer({ id: customer.id }), disabled: false })] }), model.customerCursor === null ? h.p([], ["All customer pages loaded."]) : quietButton(h, { label: "Load next customers", message: Message.ClickedNextCustomers(), disabled: Requests.pending(model.requests, "customers.list") }), h.form([h.Class("stack"), h.OnSubmit(Message.ClickedSaveTechnician())], [h.h2([], [model.editingTechnicianId === null ? "Create technician" : "Edit technician"]), model.editingTechnicianId === null ? field(h, { id: "technician-id", label: "Technician ID", children: withError(h, textInput(h, { id: "technician-id", value: model.technicianId, onInput: (value) => Message.ChangedTechnicianId({ value }), type: "text", placeholder: "tech-1", autocomplete: "off" }), model.fieldErrors.technicianId) }) : h.p([], [`Technician ID: ${model.editingTechnicianId}`]), field(h, { id: "technician-name", label: "Name", children: withError(h, textInput(h, { id: "technician-name", value: model.technicianName, onInput: (value) => Message.ChangedTechnicianName({ value }), type: "text", placeholder: "Sam Technician", autocomplete: "off" }), model.fieldErrors.technicianName) }), field(h, { id: "technician-on-call", label: "On call", children: selectInput(h, { id: "technician-on-call", value: String(model.technicianOnCall), onChange: (value) => Message.ChangedTechnicianOnCall({ value }), choices: booleanChoices }) }), h.div([h.Class("actions")], [primaryButton(h, { label: model.editingTechnicianId === null ? "Create technician" : "Save technician", message: Option.none(), type: "submit", disabled: Requests.pending(model.requests, "technicians.save") }), quietButton(h, { label: "Clear", message: Message.ClickedClearTechnician(), disabled: false })])]), dataTable(h, { caption: "Technicians loaded for selector", columns: ["ID", "Name", "On call", ""], rows: model.technicians, key: (technician) => technician.id, cells: (technician) => [technician.id, technician.name, onCall(technician.onCall), quietButton(h, { label: "Edit", message: Message.ClickedEditTechnician({ id: technician.id }), disabled: false })] }), model.technicianCursor === null ? h.p([], ["All technician pages loaded."]) : quietButton(h, { label: "Load next technicians", message: Message.ClickedNextTechnicians(), disabled: Requests.pending(model.requests, "technicians.list") })])])
] }) })
