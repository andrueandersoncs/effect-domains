# Research Agenda

This page distinguishes the implemented API from evidence still needed to judge the project hypothesis. Mechanical, lossless derivation remains the boundary; business policy and semantic transformations remain authored. ([Project thesis](raw/project-thesis.md))

## Framework Direction

The intended product is conventionally generated application infrastructure for Effect: authors declare canonical schemas, select generated resource capabilities, and write commands and policies whose semantics are not in those schemas. Current evidence includes SQLite reservation policy, tenant-scoped orders/invoices, customer-support case management, and two materially different native execution integrations: durable export/approval and scheduled per-recipient delivery. These bounded demonstrations do not establish general business-domain fit. ([Effect and declarative interface direction](raw/effect-and-declarative-interface-direction.md); [Verification limits](validation-strategy.md#evidence-boundary))

### Implemented contract

The explicit compiler cutover supersedes earlier current-contract descriptions below where they differ. Dated validation records retain the interfaces they exercised; current implementation evidence is linked from this section.

`SchemaField` compiles supported fields to `FieldIR`, retaining physical metadata, codecs, comparison evidence, and original AST. Resource capability and creation-source algebras, Command specs, recursive ReadModel syntax, FeatureFlag declarations, and Application parts are closed authoring languages. Each compiler uses exhaustive interpreters, and `Application.compile` produces the sole `ApplicationIR` consumed by adapters.

- `Resource.define({ name, schema, authorization, capabilities, storage?, relations?, version?, transitions? })` returns intent-only `ResourceSpec`. `Resource.compile` derives the runtime; narrow `Resource.table`, `repository`, `contracts`, `group`, `handlers`, and `published` helpers expose compiled products. `Resource.capabilities`, `crud()`, `get()`, `list()`, `create()`, `update()`, `patch()`, `remove()`, and `transition()` are explicit syntax.
- `effect-domains/domain` exports the shared value schemas `CalendarDateSchema`, `UuidV7Schema` (the generated identifier format), `SafeIntSchema`, `NonNegativeSafeIntSchema`, `PositiveSafeIntSchema` (SQLite INTEGER-safe bounds), and `PageLimitSchema` (1–100). `Authorization.for(...)` adds `sameAs(field)` and accepts `SubjectPolicy` values in `scope`/`allow`, merging their entitlement requirements; `includes` is literal-aware. Example roles are declared once in example-support and reused. ([Domain values](../../packages/effect-domains/src/domain.ts); [subject policy embedding](../../packages/effect-domains/src/authorization.ts); [shared roles](../../packages/example-support/src/subject.ts))
- `Resource.create({ sources })` maps fields to `defaultValue`, `generated`, or `fromSubject` nodes; remaining fields are input and nullable fields receive an implicit null source. Input schema, evaluator, and inspection use the same creation fold. `Resource.list` declares filters, ranges, fixed ordering, and limits.
- Versions and `Transitions.make` remain explicit data. Including `Resource.transition()` publishes the derived guarded mutation; cross-record side effects stay in an authored Command implementation.
- Relation names derive unless authored; foreign keys use `Resource.reference(target, fields)` with optional scope fields prepended to both sides. Application compilation requires the exact target Resource descriptor. `Table.project` remains the lower-level projection helper for authored nested queries.
- `ReadModel.define` declares source resources, explicit equality joins, and recursive selections. One `ReadModel.fold` powers compiled SQL/schema metadata and inspection/dependencies; `page`, `compilePage`, and `publish` layer bounded keyset execution and command publication without restating the contract. Exact resource identities are validated during application compilation. ([Contract](tables-and-queries.md#declared-joined-projections); [regressions](../../test/ReadModel.test.ts))
- Authorization scope is mandatory and conjoined with action rules. Hidden rows look missing; SQL filters before pagination; mutations check current/candidate state and readable results transactionally. A closed policy fold drives evaluation, SQL, references, and inspection. SQL visibility accepts native Boolean values through SQLite `0`/`1`, but rejects semantic Boolean codecs. Protected RPCs require request-local subjects through `AuthorizationRpc.Authenticator`; authored SQL remains privileged. ([Authorization contract](tables-and-queries.md#resource-authorization); [SQL interpreter](../../packages/effect-domains/src/policy-sql.ts); [authorization regressions](../../test/Authorization.test.ts); [RPC identity regressions](../../test/AuthorizationRpc.test.ts))
- Portable `effect-domains/identity` contains only credential/session/subject schemas, `IdentityUnavailable`, and the narrow `IdentityRuntime`. Native `effect-domains/identity-rpc` defines `IdentityRpcs`, `IdentityHandlers`, `authenticateIdentity(headers)`, and the composable `IdentityBundle`. `SqliteIdentity.layer({ application, accounts, password, sessionLifetime?, filename? })` is the framework credential store: account/session `Table`s prepared through the normal migration path on `SqliteBunRuntime.privateClient({ application, purpose })`, which resolves `<APP>_<PURPOSE>_DB` and refuses the application database file/inode. It remains a bounded demo store, not a production IdP. ([Portable interface](../../packages/effect-domains/src/identity.ts); [SQLite identity](../../packages/effect-domains/src/sqlite-identity.ts); [private client](../../packages/effect-domains/src/sqlite-bun.ts))
- Declarative entitlement requirements supplement, never replace, ordinary authorization. `p.entitlement({ name, key })` binds a string key; resource and subject policies share the runtime `Entitlements` service. `Entitlements.fromTables([new Entitlements.Source({ name, table, subject, key, scope, grant })])` derives table lookup from typed storage-to-subject field bindings in `scope`. `Entitlements.for(table)` builds a closed, schema-backed grant expression from typed row operands, `now`, equality/order comparisons, and `all`/`any`; status, expiration, and cancellation-grace semantics remain explicit but are no longer hidden in callbacks. Missing grants and unavailable resolvers have distinct typed failures; checks never charge or consume credits. ([Contract](tables-and-queries.md#entitlement-gating); [service](../../packages/effect-domains/src/entitlements.ts); [regressions](../../test/Entitlements.test.ts))
- `FeatureFlags.define({ name, default, description? })` produces an immutable application-level declaration. `Part.featureFlag` registers exact descriptors; nested application compilation flattens them into `ApplicationIR.featureFlags`, rejects empty or duplicate names, and exposes them through inspection. `FeatureFlags.isEnabled`, `setEnabled`, `enable`, `disable`, and atomic `toggle` program against a narrow service. `FeatureFlags.layerMemory(application.featureFlags, overrides?)` supplies process-local state and rejects undeclared exact descriptors; durable stores, targeting, rollout policy, and administration remain authored runtime concerns. ([Portable service](../../packages/effect-domains/src/feature-flags.ts); [runtime reference](../reference/runtime.md#feature-flags); [regressions](../../test/FeatureFlags.test.ts))
- `Command.define` records schemas, policy, transaction mode, and exact dependencies without a handler. `Command.implement` deliberately remains the authored Effect seam for business decisions, cross-record transitions, application SQL, and external interactions; these are semantic procedures, not mechanical representations of the contract. The compiler derives codecs, authorization, transactions, and unavailable-error translation around it. `Command.bundle` composes implementations. `Application.define` uses explicit `Part.resource`, `Part.command`, `Part.native`, `Part.featureFlag`, and `Part.application` nodes, while `Application.compile` produces the normalized group, handlers, resources, commands, feature flags, tables, and inspections consumed by every adapter.
- `ApplicationBun.run(application, { database, services?, initialize?, background?, routes?, rpc?, mcp?, ui?, telemetry? })` returns a typed Bun entrypoint `Effect`; `ApplicationBun.runInfrastructure(infrastructure, options)` preserves the compiled application's Effect error/environment contract while deriving the application, migrations, and RPC/MCP/UI settings. Persistent infrastructure uses the normal configured file; ephemeral infrastructure uses `:memory:` and rejects a filename override. `ApplicationBun.runMain` re-exports the native `BunRuntime.runMain` boundary. Startup prepares application storage and services, initializes, registers background layers, and launches HTTP or a worker lifetime. Native execution services compose in `services`; `worker` exists only with `background`, and authored HTTP `routes` run only under `serve`.
- `Infrastructure.define({ name, parts })` records a closed provider-neutral resource graph. HTTP/background/scheduled runtimes, SQLite stores, durable filesystems, object stores, queues, secrets, variables, endpoints, domains, OTLP destinations, and explicit extensions are distinct syntax nodes; runtime bindings preserve exact descriptor identity. `InfrastructureCompiler.compile` requires canonical unpadded names and IDs, validates registration and acyclicity, rejects duplicate publication tags and collisions with every route reserved by the generated UI, derives exact writer-topology and backup-schedule capabilities, and assigns stable `<application>/<resource>` logical IDs. `InfrastructureInspect.describe` emits canonical data without handlers or credentials. `ApplicationInfrastructure.define` mechanically derives the SQLite/HTTP/publication shape while preserving the exact compiled application type through `InfrastructureIR`.
- `ApplicationRuntime` owns the portable context and HTTP assembly used by Bun and deployed Node processes. `SqliteBunRuntime` and `SqliteNodeRuntime` share one repository/migration implementation while supplying platform drivers. Alchemy Railway and Fly runtimes build that context once at process acquisition rather than rebuilding storage, migrations, services, and routes per request. Both support only the declared single-writer topology and reject multi-writer graphs. Railway accepts `backups: "none"` because its resource API cannot apply a schedule; Fly maps `none`, `daily`, and `weekly` to volume snapshot settings. Non-production stacks default to local Alchemy state; a production stage defects before provider resources execute unless `options.state` supplies a nonlocal state layer. Capability validation rejects Cloudflare before resource declaration because request execution and D1 batch transactions cannot preserve this process/interactive-transaction graph.
- `ApplicationBun.run` also accepts declarative project-owned telemetry for OTLP traces, metrics, and logs. One resource identity and scoped runtime cover server, remote CLI, Application UI/MCP dispatch, initialization, and worker lifetimes; the generated browser uses a persistent runtime and same-origin bounded gateway. Signal-specific endpoints/protocols/headers/batching, root sampling, runtime metrics, safe HTTP/RPC histograms, browser selection, and shutdown timeouts are explicit. Runtime-owned HTTP spans suppress native URL/query/header capture. No endpoint means no runtime-owned export; `telemetry: false` opts out without replacing external telemetry. ([Runtime reference](../reference/runtime.md#opentelemetry); [telemetry layer](../../packages/effect-domains/src/application-telemetry.ts); [validation](validation-strategy.md#2026-09-15-full-stack-observability-and-durable-audit-evidence))
- `serve` derives Streamable HTTP MCP at `/mcp` and can enable the generated Application UI with `ui: true` or `{ path?, presentation?, allowedOrigins? }`, defaulting to `/`. The workspace builds [`apps/application-ui`](../../apps/application-ui/) explicitly; runtime loads only its prebuilt assets. `ApplicationUi` interprets the authoritative `ApplicationIR` inspection into operation forms, resource lists, declared filters, cursor navigation, and complete-JSON input. Every maintained example uses this interpreter instead of an authored browser application.
- Application UI and MCP invoke the same request-isolated in-process RPC transport. Captured handler services preserve service-dependent codecs; authentication remains request-local. A browser token stays in memory, a successful `identity.login` result populates it, and logout clears it. Presentation remains display-only configuration and never changes inspection, authorization, or handlers. ([Transport](../../packages/effect-domains/src/rpc-in-process.ts); [UI regressions](../../test/ApplicationUi.test.ts))
- CLI input uses only `--input-json`; no-payload input is schema-derived, while empty structs and all-optional payloads default to `{}`. Native subcommands, help, and inspection remain. ([CLI regressions](../../test/RpcCli.test.ts))
- `SqliteMigrations.make({ id, to, steps })` constructs an explicit v2 artifact without a `from` snapshot; `CreateTable`, `RebuildTable`, and `CreateIndex` steps reference `to.tables` by name and rebuilds list only non-identity copies. `initial({ id, tables })` derives fresh creation; `history(...artifacts)` decodes and validates synchronously, `decodeHistory(raw)` as an Effect. There is no `plan`, `generate`, intent DSL, schema CLI, or v1 ledger compatibility. Example artifacts shrank from 4,938 to 2,665 JSON lines. ([Migrations](../../packages/effect-domains/src/sqlite-migrations.ts); [migration contract](tables-and-queries.md#sqlite-migrations-and-inspection))
- `inspect` emits generated operation input/output/error schemas, registered feature-flag names/defaults/descriptions, and resource canonical schema, creation/list policy, including implicit UUID generation and rendered `fromSubject` mappings, storage schema, and the physical table as JSON-encoded `Table.snapshot`; it does not maintain a duplicate physical DTO. It is the descriptive input to the Application UI, while presentation stays external. Handler services, live flag state, and transaction boundaries remain deliberately uninspectable rather than invented metadata. ([Inspection](../../packages/effect-domains/src/application-inspect.ts); [Table](../../packages/effect-domains/src/table.ts); [UI adapter](../../packages/effect-domains/src/application-ui.ts))
- Native workflow/entity proxy bundles compose directly in `Application.parts`; native registration and queue-worker layers compose in `background`. Applications supply private execution SQL with `Layer.provide` when composing native execution layers into `services`, without exporting that SQL as application SQL. The shared example helper checks opened database paths/inodes before native execution tables initialize; it does not promise pre-open URL or dangling-symlink rejection. Application Entity/Cron work captures its application database connection before native Sharding supplies its invocation context. The workflow application owns its diagnostic schemas and reads native `Sharding` and `RunnerStorage` services directly. ([Workflow runtime](../../examples/report-exports/runtime.ts); [reminder registration](../../examples/appointment-reminders/background.ts); [diagnostics](../../examples/report-exports/workflow.ts))

Publication and runtime configuration remain outside canonical schemas. Intrinsic identity is domain metadata; generated persistence identity is an adapter concern. Native RPC declarations belong to the operation-contract layer and do not select HTTP transport; applications compose parts and may opt into the generated UI without adding presentation metadata to domain schemas. The clean cutover leaves no legacy authored browser client framework or compatibility adapter. ([Domain identifier direction](raw/domain-identifier-and-basic-persistence-direction.md); [Application](../../packages/effect-domains/src/application.ts); [Bun runtime](../../packages/effect-domains/src/application-bun.ts))

## Established Persistence Findings

`Table.make` compiles supported flat canonical fields to reversible storage codecs and SQLite fields. An explicit `identifier` remains the key; otherwise persistence adds UUIDv7 `id` to the row representation. The compiler supports string, numeric, boolean, nullable scalar, and timestamp representations, not arbitrary nested or optional-key models. ([Table](../../packages/effect-domains/src/table.ts); [table regressions](../../test/Table.test.ts))

Compiled column codecs preserve suspended scalar mappings and exact-filter semantics; nullable physical identifiers are rejected. Canonical JavaScript string-length checks are not SQLite constraints. Explicit example rebuilds remove old misderived constraints without reinterpreting frozen history, and interacting renames copy original source values. ([Persistence contract](tables-and-queries.md#canonical-and-storage-representations); [migration contract](tables-and-queries.md#sqlite-migrations-and-inspection))

Table/Resource `relations` now declares named composite uniqueness, foreign keys, and ordinary secondary indexes outside canonical schemas. Complete-graph validation checks physical targets; SQLite enables foreign keys and migration history manages constraint rebuilds and explicit index steps. No join or cascade policy is inferred. ([Relational contract](tables-and-queries.md#relational-storage-declarations); [migration regressions](../../test/RelationalMigrations.test.ts))

Generated repositories handle routine persistence; authored operations use native Effect `SqlSchema` and `SqlClient`, or explicit schema conversions. There is no framework `Query` descriptor or persistent-reference cache. Transactions, caching, invalidation, and concurrency policy remain explicit application concerns. ([Authored SQL](../../examples/expense-ledger/sqlite.ts); [transactional reservation policy](../../examples/reservations/sqlite.ts))

The example abstraction pass reuses those existing repositories for expense create/find/update, native SQL fragments for range predicates, and `Schema.fromJsonString` for billing projections. Private SQLite composition and atomic file replacement are shared example-support utilities, not framework execution or filesystem policy. The user approved removing mandatory unused schema interfaces by retiring only `schema-record-interface`; all other lint rules remain enabled. ([Expense SQL](../../examples/expense-ledger/sqlite.ts); [billing SQL](../../examples/orders-invoices/sqlite.ts); [example support](../../packages/example-support/src/); [lint configuration](../../better-typescript.json); [verification](validation-strategy.md#2026-09-10-shared-example-abstractions))

The orders/invoices `NestedRow.make({ table, fields })` helper is explicitly example-local. It derives nested JSON selection keys and result codecs from compiled column storage schemas, including physical Boolean/timestamp forms; it does not introduce an ORM or automatic relationship planner. The reservations resource keeps a private generated creation policy (`held`, UUIDv7, current timestamp) for its authored reserve transaction, with no public `reservations.create` operation. ([Billing projection](../../packages/effect-domains/src/table.ts); [reservation resource](../../examples/reservations/resources.ts); [reservation policy](../../examples/reservations/sqlite.ts))

## Application Evidence

Examples are domain-first applications rather than framework-feature fixtures: reading lists, expenses, team tasks, field reports, editorial planning, equipment records, reservations, billing, customer-support cases, financial exports, and appointment notifications. Basic examples remain generated CRUD where editing records is the actual use case; authored queries, business commands, and durable workflows remain explicit where semantics require them. Each application owns its canonical schema. ([Application guide](../../examples/README.md); [Support-case policy](../../examples/support-cases/sqlite.ts); [Reservation policy](../../examples/reservations/sqlite.ts); [Expense queries](../../examples/expense-ledger/sqlite.ts))

Team tasks preserve tenant/owner visibility and completion locks. Shared field notes use reader/editor/admin role membership independently of their encrypted text codec. The shared example identity store issues and verifies opaque credentials against current account state, but it is not a production user-management system or IdP. ([Task rules](../../examples/team-tasks/resources.ts); [Field-note rules](../../examples/field-notes/resources.ts); [Example identity](../../packages/example-support/src/identity.ts))

Every maintained application enables the generated Application UI at `/`; Support Cases adds presentation metadata while retaining the loopback origin default. Equipment registration is also exercised by an official MCP SDK client. Field-note storage replaces only the canonical body field with an explicit service-dependent encryption codec; it is not full-database encryption. ([Support runtime](../../examples/support-cases/main.ts); [Support walkthrough](../../examples/support-cases/README.md#use-the-application-ui-or-mcp); [Field-note codec](../../examples/field-notes/storage.ts))

The dated [authorized-example verification](validation-strategy.md#2026-09-09-authorized-examples) and [authorization verification](validation-strategy.md#2026-09-08-resource-authorization) remain historical evidence for their exercised policy, codec, migration, identity-isolation, SQL-parity, and type-boundary interfaces. They are not a claim that later implementation changes have been validated.

Report exports compose approval, durable timing, an activity, queue-backed JSON artifact creation, and an application-owned operator audit trail for release/recovery/cancellation/reconciliation. Appointment reminders compose persisted per-recipient scheduling, deduplicated in-application notifications, a singleton projection, and cron retention. Earlier generic export/reminder fixtures exercised forced restart and worker recovery; those dated observations are not automatically evidence for every new domain rule. Neither application claims external email/SMS delivery, production identity, or a generic audit framework. ([Historical durable verification](validation-strategy.md#2026-09-09-native-durable-execution); [report runbook](../../examples/report-exports/README.md); [report audit](../../examples/report-exports/audit.ts); [reminder runbook](../../examples/appointment-reminders/README.md))

Report generation now requires an editor role and a current account subscription. Authored native RPC handlers bind workflow identity to the authenticated account while preserving the full report input; global admin operators retain release, recovery, polling, and metrics access independently of payment. Purchased guides demonstrate a distinct per-user, per-resource grant checked after tenant visibility. Both resolvers read persisted application facts on each check, and idempotent seeds preserve expiry and refunds across restart. These examples do not implement a billing provider, webhook reconciliation, checkout, quotas, or distributed entitlement consistency. ([Report policy](../../examples/report-exports/authorization.ts); [account binding](../../examples/report-exports/workflow.ts); [guide policy](../../examples/purchased-guides/resources.ts); [verification](validation-strategy.md#2026-09-10-entitlement-gating))

Orders/invoices is a second business-policy slice: authenticated tenant-local order numbers and invoice numbers, order lines, single-invoice issuance, payment transitions, checked integer totals, and explicit expected-version writes across three tables. Its three resources and authored command bundle form a nested billing application; the outer runnable application adds native identity, and `Application.compile` flattens both into the one adapter-facing IR. Generated reads remain tenant-scoped; mutation RPCs use authored SQL and transactions. Live concurrent writes produced one commit and one `VersionConflict`; a failed invoice insert rolled back the order transition in regression coverage. This demonstrates the chosen billing rules and nested composition with a native sibling, not accounting compliance, payment processing, inferred aggregate boundaries, or universal optimistic locking. ([Application](../../examples/orders-invoices/application.ts); [Domain](../../examples/orders-invoices/domain.ts); [contracts](../../examples/orders-invoices/contracts.ts); [SQL](../../examples/orders-invoices/sqlite.ts); [composition verification](validation-strategy.md#2026-09-14-nested-application-example-coverage); [billing verification](validation-strategy.md#2026-09-09-tenant-scoped-orders-and-invoices))

Repair workshops add a distinct read-model slice: a board of repairs joined to current customers and optional technicians, preserving booleans and missing-row nulls. The runnable handwritten baseline preceded the compiler; the final example removes its repeated storage projection and JOIN/alias SQL while preserving the native operation's behavior. This is evidence for flat joined projections and declared dependencies, not a staffing scheduler, a production authorization system, or general aggregate inference. ([Application](../../examples/repair-workshop/README.md); [board](../../examples/repair-workshop/board.ts); [verification](validation-strategy.md#2026-09-11-repair-workshop-joined-projections))

Support cases add a third business-policy slice and a second joined read model. `SupportDirectory` and `CaseManagement` are sibling applications: case foreign keys, the board, and authored command dependencies consume exact Resource descriptors registered by the directory sibling, and final compilation validates them after flattening the complete tree. Cases declare versions and `open → triaged → assigned → resolved`; assignment requires a current on-duty agent. Transactional Command implementations update the case, append its event, and append a private lifecycle audit record together. The audit action is a closed domain schema, actor comes from the authenticated subject when present, and ordinary CRUD publication is disabled. The board uses `ReadModel` for the flat customer/optional-agent projection, while nested event/audit history remains authored SQL because aggregation and retention semantics cross the current boundary. ([Application](../../examples/support-cases/application.ts); [audit](../../examples/support-cases/audit.ts); [policy](../../examples/support-cases/sqlite.ts); [validation](validation-strategy.md#2026-09-15-full-stack-observability-and-durable-audit-evidence))

## Adjacent Native API Decisions

These are scoped integration decisions, not claims that unused APIs were validated:

- Use native `PersistedQueue` for durable handoff; use `DurableQueue` when completion must resume a workflow. The export example composes the latter over the former's SQL store, without another worker registry. ([Composition](../../examples/report-exports/runtime.ts))
- Use Entity only where identity-local serialization and scheduling are meaningful. `PrimaryKey`, `DeliverAt`, Singleton, and ClusterCron remain native and explicit; do not turn every Resource into an actor. ([Reminder protocol](../../examples/appointment-reminders/appointment-reminder-entity.ts); [background work](../../examples/appointment-reminders/background.ts))
- Keep RateLimiter at an explicitly chosen application boundary; do not infer durable business quotas or add a framework wrapper without a concrete requirement.
- Use EventLog/EventJournal only after a synchronization slice defines its semantics. Field Notes now proves typed SQL journals, replica replay, duplicate handling, restart rebuild, and deterministic `(revision, replicaId)` conflict resolution. This does not turn existing resources into event-sourced aggregates, audit logs, or outboxes. ([Synchronization slice](../../examples/field-notes/sync.ts))
- Keep interactive cache and synchronization policy outside canonical Resource schemas. The generated Application UI performs ordinary operation calls and explicit resource reloads; Field Notes' EventLog remains a separate application-level replica policy. Server push, cross-client synchronization, and offline state are outside the UI contract. ([UI contract](tables-and-queries.md#commands-applications-and-application-ui))
- Multi-runner topology, execution-store upgrades, cancellation, outbox dispatch, and external-effect idempotency remain authored operational concerns. The report slice now proves two colocated HTTP runners sharing SQLite, failover, durable app-owned acceptance, recoverable suspension, cancellation, immutable artifact reconciliation, and startup topology rejection. It does not establish cross-host SQLite, cross-database atomicity, or general exactly-once effects. ([Runtime contract](tables-and-queries.md#native-durable-execution); [report runbook](../../examples/report-exports/README.md))

## Current Direction

Human direction on 2026-09-11 sequenced the remaining questions. The subsequent support-case and native Cluster/synchronization slices record the next results without treating the broader hypothesis as proven.

- Stay on SQLite. The support-case slice adds another materially different business domain without requiring framework changes; further evidence should continue to test the existing seams rather than add options pre-emptively.
- Joins and multi-table query dependencies were the next declaration work. Repair workshops and support cases now exercise two explicit flat projections with registered dependencies; neither infers joins from foreign keys. Support-case event history stayed authored SQL because nested aggregation is not a mechanical flat projection. Physical uniqueness, foreign keys, and indexes stay; cascade and richer relationship policy stay out until a slice needs them. ([Relational contract](tables-and-queries.md#relational-storage-declarations); [implemented projection boundary](tables-and-queries.md#declared-joined-projections))
- Where a canonical value has no lossless scalar representation, keep an explicit typed transform per field. Do not infer a JSON blob or other non-lossless encoding. This restates the derivation boundary. ([Thesis](thesis.md#derivation-boundary))
- The verified issuance/revocation slice is complete and now lives in the framework as `SqliteIdentity`: credentials are issued, expire, revoke, and resolve current account claims without placing identity in canonical resource schemas. Keep the portable subject-only authorization boundary; a production IdP or user-management lifecycle requires its own requirements and evidence. ([Identity runtime](../../packages/effect-domains/src/identity.ts); [SQLite identity](../../packages/effect-domains/src/sqlite-identity.ts))
- Expand mechanical storage support only when a slice cannot stay honest with the current scalar subset.
- Migration authoring changed once the hand-written histories measurably hurt: twenty artifacts carried every snapshot two or three times and ~30 identity column copies, so the v2 artifact drops the redundant `from`, references snapshots by name, and defaults identity copies. Steps, constants, and expressions remain explicit; there is still no planner, generator, or dialect work. The three `003_schema_string_checks` rebuilds stay as frozen history. ([Migration contract](tables-and-queries.md#sqlite-migrations-and-inspection))
- Guarded writes, status machines, and authored-operation plumbing are derived where repeated code is mechanical: version guards, declared transitions, `UniqueViolation` translation, keyset list ranges/ordering, seeds, entitlement lookups, and operation contracts. Support cases reused versioned transitions, range/order, a joined board, and transactional side effects without expanding those declarations. Business rules and nested event-history SQL stayed authored. The next domains should keep testing whether these boundaries hold without accreting options. ([Support-case validation](validation-strategy.md#2026-09-13-support-case-sqlite-vertical-slice); [abstraction-pass validation](validation-strategy.md#2026-09-11-abstraction-pass))
- Interruption and ambiguous commits require application-owned policy, not a framework default. Report exports now use an application transaction for acceptance/outbox state, deterministic at-least-once native dispatch, explicit cancellation/recovery states, and idempotent immutable artifact reconciliation. Support-case trigger rollback remains ordinary same-database transaction evidence; neither slice establishes a universal cross-system policy. ([Cluster and reconciliation validation](validation-strategy.md#2026-09-14-native-cluster-completion-and-synchronization-slices))

## Remaining Evidence

Still unproven after those choices:

- whether further SQLite domains continue to generalize the conventions without framework growth;
- whether the bounded joined-projection declaration generalizes beyond two flat boards without becoming a general query DSL;
- whether aggregate or nested read models have a repeatable mechanical subset beyond authored SQL;
- whether a production IdP or user-management lifecycle is required beyond the bounded example credential store;
- which encoded shape, if any, a future slice cannot avoid;
- whether declared `version`/`transitions`/`range`/`order` cover substantially different domains without growing into a query or workflow language;
- readability of large or database-specific v2 histories, when one hurts;
- how application-owned reconciliation should handle ambiguous commits for non-idempotent external services beyond the demonstrated immutable file sink;
- whether production collector/storage topology, capacity planning, SLO thresholds, and retention preserve the local tri-signal contract under real load;
- whether a future audit domain shares enough actor/action/target, retention, and privacy semantics to justify any framework abstraction beyond the two authored tables;
- whether Railway or Fly provider APIs, supplied remote state, backup restoration, rollout behavior, and regional failure handling preserve the inspected deployment plans in a live production stack;

The table compiler directly interprets supported Effect AST nodes and rejects cyclic suspended scalar fields. Its storage semantics remain separate from canonical JSON RPC codecs; CLI support does not imply arbitrary schemas have lossless table representations. ([Table compiler](../../packages/effect-domains/src/table.ts); [table regressions](../../test/Table.test.ts); [CLI](../../packages/effect-domains/src/rpc-cli.ts))

## Success and Stop Conditions

The SQLite implementation removes exercised mechanical duplication, native execution examples avoid another workflow/actor language, and the identity slice establishes bounded example issuance/revocation without loading sessions into canonical schemas. Support cases reused the existing version, transition, operation, projection, range, ordering, and transaction seams without framework additions; nested history remained authored SQL. The report failure slice now demonstrates same-execution recovery and one idempotent external effect. Further architectural claims still require richer SQLite domains, joined projections beyond the two demonstrated flat read models, cross-host execution evidence, and ambiguous-commit policy for non-idempotent external systems. Compare propagation, annotation cost, escape hatches, and clarity against the [validation criteria](validation-strategy.md) before generalizing another framework abstraction.
