# Validation Strategy

## Goal

Test Effect Domains through complete vertical slices that expose the difference between domain behavior, storage, and transport. A schema demonstration alone cannot establish useful conventions. ([Project thesis](raw/project-thesis.md))

## Required Shape of a Slice

Each materially different slice should include branded values, a meaningful transition, typed business failures, database and wire representations, explicit transformations where semantics differ, and historical migration. Record removed duplication, change propagation, annotation cost, runtime boundaries, escape hatches, and clarity against handwritten adapters. ([Project thesis](raw/project-thesis.md); [Declarative interface direction](raw/effect-and-declarative-interface-direction.md))

## Evidence Standard

Link implementation and regressions, distinguish authored policy from generated machinery, and date live observations. A successful scenario establishes only its exercised contract. Historical verification is retained as historical evidence when the interface changes. This reporting rule is wiki analysis based on the thesis evaluation criteria.

## Reservation Slice

The [reservation application](../../examples/reservations/README.md) has branded SKU and reservation identifiers, positive quantities, and explicit `held → confirmed | released` transitions. Canonical models contain no transport or persistence mapping annotations beyond intrinsic identity. ([Domain](../../examples/reservations/domain.ts))

| Concern | Implementation |
| --- | --- |
| Models and policy | Canonical schemas, reserve/confirm/release contracts, transitions, guarded stock accounting, and transactions are authored |
| Storage | Resource tables, recognized constraints, and reversible codecs are generated |
| Publication | Only selected resource reads are published; reservation mutations remain business commands |
| HTTP, CLI, and MCP | Application groups provide RPC dispatch, canonical JSON CLI input, and derived MCP tools |
| History | Frozen artifacts preserve historical schema and explicit timestamp conversion |

Sources: [resources](../../examples/reservations/resources.ts), [application](../../examples/reservations/application.ts), [contracts](../../examples/reservations/sqlite.ts), [policy](../../examples/reservations/sqlite.ts), [migration fixture](../../examples/reservations/migrations.ts), and [runtime](../../packages/effect-domains/src/application-bun.ts).

The slice removes duplicate storage schemas, ordinary read/query plumbing, initial DDL, and per-operation HTTP/CLI binding; stock effects and transaction boundaries remain authored. It is an SQLite-only result, and generated JSON CLI commands are not evidence that generated CLIs replace domain-specific CLI design. ([Reservation regressions](../../test/Reservations.test.ts); [CLI regressions](../../test/RpcCli.test.ts))

## Verification Record

### 2026-09-14: Declarative browser results and entitlement scopes

The browser RPC boundary now derives success and failure envelopes from declared message schemas. Authored query, command, and mutation Effects return only success payloads; `RpcBrowser` constructs the declared messages, injects request ownership for commands and mutations, derives ordinary error text, and requires `failurePayload` only when the failure schema carries additional fields. Every example RPC envelope, `IdentitySession`, and generated `ResourceEditor` now uses that contract. Table-backed entitlement sources similarly replace executable subject-to-SQL callbacks with typed storage-field-to-subject-field `scope` bindings; the semantic row/time grant predicate remains authored. `Command.implement` remains the intentional Effect seam for business policy and transactions rather than being replaced by a workflow DSL. ([Browser RPC](../../packages/effect-domains/src/rpc-browser.ts); [entitlement source](../../packages/effect-domains/src/entitlements.ts); [commands](../../packages/effect-domains/src/command.ts); [runtime reference](../reference/runtime.md))

- The browser regression failed before the RPC cutover because the framework still invoked an authored `onSuccess` callback. It now covers schema-derived query and mutation results, automatic mutation invalidation/refetch, request-token injection, default failures, and explicit field-error payloads. The entitlement regression failed before its cutover because the resolver still invoked `where(subject)`; it now proves a declared tenant binding grants the matching active record while denying an inactive record, a different tenant, and an unknown source. Compiler fixtures reject missing or type-incompatible scope bindings. ([Browser regression](../../test/RpcBrowserReactivity.test.ts); [entitlement regression](../../test/Entitlements.test.ts); [entitlement compiler fixture](../../test/Entitlements.types.ts))
- Focused verification passed **17 tests in 5 files** across browser RPC, generated editors, entitlements, commands, and identity. Complete workspace verification passed `bun run check`, `bun run lint`, **143 tests in 30 files** through `bun run test`, `bun run build`, and `bun run docs:build`.
- A rebuilt Reservations server used isolated SQLite storage. With the documented timer-backed frame pump for the headless harness, the real Foldkit page rendered `book: 2 available`; reserving one unit rendered `Reservation held.`, the held reservation, and the automatically refreshed `book: 1 available`. The request sequence exercised schema-derived success messages and mutation invalidation.
- A rebuilt Equipment Register server used separate isolated SQLite storage. Submitting its empty registration form rendered the RPC failure notice and the asset-tag field error from the command's explicit `failurePayload`, exercising the additional-fields escape hatch on the actual surface. The tabs, servers, and disposable databases were removed afterward.

This proves the declared result-envelope contract over local unary RPC, browser-local invalidation, and typed equality scoping for SQLite entitlement sources. It does not derive presentation, business transitions, transactions, arbitrary error decomposition, non-equality SQL scopes, server push, cross-tab invalidation, or a general workflow language. Those semantics remain explicit.

### 2026-09-14: Configured admin presentation and composed telemetry

Support Cases supplies the first maintained example of the `ApplicationBun.run` admin object form. Its runtime selects `/support-admin` and supplies external presentation metadata for the application title, resource labels and columns, plus authored-operation labels and descriptions. The same nested application supplies explicit OTLP HTTP/protobuf resource metadata; neither configuration changes its canonical schemas, application inspection, authorization, or handlers. ([Support runtime](../../examples/support-cases/main.ts); [walkthrough](../../examples/support-cases/README.md#use-the-browser-admin-or-mcp); [admin adapter](../../packages/effect-domains/src/application-admin.ts); [telemetry adapter](../../packages/effect-domains/src/application-telemetry.ts))

- The Support Cases package typecheck and Better TypeScript lint passed, and both browser bundles rebuilt.
- A real server used isolated SQLite storage on an alternate port. `/support-admin` and its metadata endpoint returned HTTP 200 while the default `/admin` returned 404. A headless browser rendered **Support operations**, the four configured resource labels, the four authored-operation labels, selected `id`/`name` customer columns, and the Open Case description and generated form.
- A local collector received OTLP `application/x-protobuf` exports containing the configured `support-cases` service name and `effect-domains.examples` namespace after admin requests and a successful remote `support.board` CLI call. Graceful server interruption emitted final exports; the collector, browser, server, and disposable storage were removed.

This proves configurable path/presentation consumption on a real composed application and explicit protobuf telemetry across its server and CLI lifetimes. It does not exercise a non-loopback `allowedOrigins` deployment, collector authentication, production sampling, metrics, or logs. The origin option remains omitted deliberately so alternate loopback ports retain the adapter's safe default.

### 2026-09-14: Resource editor query forms and worker telemetry

`ResourceEditor` now requires a reversible query-form codec beside its edit-form codec. The editor owns query state, validates individual query-field changes through the encoded schema, replaces an obsolete page when a query changes, decodes the canonical list input before each request, and preserves that query across cursor continuation. Reading List replaces its bespoke public CRUD reducer with this derived editor while retaining its authored status/format filters and nullable rating behavior; Editorial Calendar supplies the empty-query case. ([Editor](../../packages/effect-domains/src/resource-editor.ts); [Reading List client](../../examples/reading-list/web/main.ts); [Editorial Calendar client](../../examples/editorial-calendar/web/main.ts))

- Focused `ResourceEditor` coverage changes a query field after a populated page and observes the validated query plus cleared items/cursor. `Form.nullableInteger` coverage distinguishes blank-to-null, valid integers, zero/range rejection, fractional rejection, and null-to-blank encoding. The focused run passed **10 tests in 2 files**; framework, Reading List, and Report Exports package typechecks/lints passed, and both browser bundles rebuilt.
- A real Reading List server used isolated SQLite storage. Its rebuilt Foldkit surface created a rated `reading` ebook and a second `planned` paperback with blank optional values. Selecting `planned` immediately replaced the table with only the matching book—without clicking **Reload**—and the blank rating rendered as `—`. The browser tab, server, and disposable database were removed afterward.
- Report Exports adds the second example-owned telemetry declaration: OTLP HTTP/JSON, service name `report-exports`, and `service.namespace=effect-domains.examples`. A local collector received `application/json` export from `report-exports inspect`, including `ApplicationBun.inspect` and `ApplicationBun.run`.
- The same collector then wrapped a real Report Exports worker using isolated application, execution, and identity databases. It received the background SQL polling spans during the worker lifetime and, after graceful interruption, the final `ApplicationBun.runtime`, `ApplicationBun.worker`, and `ApplicationBun.run` spans under the declared resource. The collector, worker, and disposable storage were removed afterward. ([Report runtime](../../examples/report-exports/main.ts); [walkthrough](../../examples/report-exports/README.md#opentelemetry-traces))
- Complete workspace verification passed `bun run check`, `bun run lint`, **142 tests in 30 files** through `bun run test`, `bun run build`, and `bun run docs:build`.

This proves a second, filtered `ResourceEditor` consumer and a materially different telemetry lifetime. It does not derive arbitrary query UIs, authentication, server push, optimistic edits, worker metrics/logs, or a production collector deployment. The worker trace used empty queues; durable workflow correctness remains covered by the separate Cluster record.

### 2026-09-14: Nested application example coverage

Orders/invoices defines its three generated resources and authored command bundle as a nested billing application, then composes that domain module with the native identity bundle at the outer runnable boundary. Support cases adds a materially different shape: directory and case-management sibling applications whose foreign keys, joined read model, and command dependencies cross the child boundary. Both compile into the one `ApplicationIR` consumed unchanged by local tests and generated clients. ([Billing composition](../../examples/orders-invoices/application.ts); [support composition](../../examples/support-cases/application.ts); [application compiler](../../packages/effect-domains/src/application.ts))

- The first support-case runtime attempt exposed a real compiler defect: `ApplicationPart` recursively compiled and validated each child in isolation, so the case child rejected the customer table supplied by its directory sibling. Compilation now recursively collects leaf parts and performs relation, dependency, and operation validation once over the complete root tree.
- Focused Application, Orders/Invoices, and Support Cases regressions passed **6 tests in 3 files**. Application regressions now require both a foreign key and a command dependency to resolve across sibling applications while still rejecting structurally equal replacement descriptors. The domain suites prepare flattened tables, install flattened handlers, and exercise generated resources plus authored commands through the composed applications. ([Application regressions](../../test/Application.test.ts); [billing regression](../../test/OrdersInvoices.test.ts); [support regression](../../test/SupportCases.test.ts))
- `bun run check` and `bun run lint` passed every workspace package and the root fixtures. `bun run test` passed **140 tests in 30 files**, and `bun run docs:build` rendered the updated documentation.
- The real `orders-invoices --help` command exposed six generated resource operations, five authored billing operations, and three native identity operations through one nested application. The real `support-cases --help` command exposed sixteen generated resource operations and four authored board/lifecycle operations across its two sibling children.

This proves one resource-and-command child composed with a native sibling, plus two domain siblings with cross-child relational and command dependencies. It does not establish dynamic module loading, independently versioned modules, duplicate-name resolution, or deployment isolation; collisions remain compile-time errors rather than a namespacing system.

### 2026-09-14: Better TypeScript lint completion

The explicit compiler architecture now passes Better TypeScript across the framework, adapters, examples, and root integration fixtures without rule changes or exclusions. The cleanup removed the 296 diagnostics recorded at the compiler cutover: speculative exports and aliases were deleted, closed one-owner abstractions were collapsed, optional and unused type fields were removed, Effect collection and equality operations replaced native equivalents, and repeated derived values were named rather than recomputed through nested calls. Canonical domain models and declarative specifications did not gain persistence, transport, UI, or authorization concerns. ([Resource](../../packages/effect-domains/src/resource.ts); [Application](../../packages/effect-domains/src/application.ts); [compiler cutover](#2026-09-14-explicit-compiler-architecture-cutover))

- `bun run lint` passed every workspace package, all thirteen examples, both browser packages, and the root suite with the existing configuration.
- `bun run check` passed every workspace package and the root compiler fixtures. `bun run test` passed **140 tests in 30 files**.
- `bun run build` precompiled the admin and example-web applications, and `bun run docs:build` rendered the documentation site.
- A compile-cache smoke check established both sides of the identity contract: compiling the same Resource specification returns the cached runtime, while a separately authored structurally equal specification compiles to a distinct runtime. Application regressions separately reject foreign-key and command dependencies built from an unregistered descriptor.

This closes the cutover's lint debt. It does not expand the framework's database, transport, distributed-compilation, or browser-reactivity claims.

### 2026-09-14: Explicit compiler architecture cutover

The compiler boundary now separates author intent from derived runtime values. `Resource.define`, `Command.define`, `ReadModel.define`, and `Application.define` produce inspectable specifications; explicit compilation derives Resource runtime products and the sole `ApplicationIR`. Resource creation sources, capabilities, command dependencies, application parts, schema fields, and recursive read-model syntax use closed tagged vocabularies with exhaustive folds. Exact Resource identity is retained for relation and dependency validation. The superseded Operation, SqliteView, schema-algebra, and implicit application/resource constructor paths were deleted rather than retained as compatibility layers. ([Resource](../../packages/effect-domains/src/resource.ts); [Command](../../packages/effect-domains/src/command.ts); [ReadModel](../../packages/effect-domains/src/read-model.ts); [Application](../../packages/effect-domains/src/application.ts); [schema fields](../../packages/effect-domains/src/schema-field.ts))

- Focused Application, Command, ReadModel, and SchemaField regressions passed **18 tests in 4 files**. They cover authoring-specification purity, exact Resource references, invalid joins at the compiler boundary, multiple fold interpretations, command failure contracts, and application composition.
- The complete regression suite passed **140 tests in 30 files**. During the full run, stale RPC CLI fixtures were migrated to the required Application boundary, and Resource relation compilation was corrected to preserve an absent foreign-key declaration instead of materializing an empty array that changed frozen migration snapshots.
- `bun run check`, `bun run build`, and `bun run docs:build` passed. `bun run lint` did not pass: Better TypeScript reported 296 diagnostics concentrated in the new type-level compiler modules; no lint rules or exclusions were changed. Passing behavior and type checks therefore do not claim current lint compliance.
- A real Reading List server used an isolated SQLite file. Its compiled CLI created a book and its compiled read model returned two persisted rows, including an earlier successful browser form submission. A headless browser observed the RPC response and rendered both rows after replacing the harness-frozen `requestAnimationFrame` queue with a timer-backed frame pump, matching the established Foldkit browser-smoke constraint. The server, browser tab, and isolated database were removed afterward.

This verifies the explicit compiler cutover and the listed SQLite/unary RPC paths. It does not establish another database backend, streaming RPC, distributed compilation, or a lint-clean implementation.

### 2026-09-14: Shared SQLite list kernel

Generated Resource lists and declared joined views compiled through one private SQLite list kernel instead of independently implementing payload fields, storage encoding, cursor binding, page lookahead, continuation construction, predicates, and ordering. `RepositoryStore.select` used the same renderer while retaining policy and execution. The current successor is `ReadModel`. ([Kernel](../../packages/effect-domains/src/sqlite-list.ts); [Resource](../../packages/effect-domains/src/resource.ts); [current ReadModel](../../packages/effect-domains/src/read-model.ts); [store](../../packages/effect-domains/src/sqlite-bun.ts))

- Focused Resource, `SqliteView`, and Support Cases regressions passed **25 tests in 3 files**, covering equality/range encoding, ascending and descending continuation, cursor contract and request-state rejection, storage codecs, joined projection decoding, and policy-backed table execution.
- A disposable in-memory Support Cases application traversed two generated `support_customers.list` pages and two filtered joined `support.board` pages with limit one. It returned distinct Resource identifiers `acme`, `beta` and joined subjects `Second case`, `First case`, with a cursor only on each first page.
- Framework package typecheck and lint passed. This proves the exercised SQLite list contracts share mechanics without changing their caller-owned semantics; it does not establish a dialect-neutral query compiler, joins for Resource, or authorization for privileged views.

### 2026-09-14: Declarative Foldkit reactivity cutover

The browser boundary now owns automatic local refetch as declarative client mechanics rather than reducer ceremony or an isolated prototype. `RpcBrowser.query` maps an authored dependency schema, Effect, and reactivity keys to a Foldkit subscription backed by native `Reactivity.stream`; `RpcBrowser.mutation` invalidates declared keys only after success. `BrowserRuntime.run` installs one shared Reactivity service automatically. The old Field Notes-only AtomRpc slice was removed. ([Browser RPC](../../packages/effect-domains/src/rpc-browser.ts); [browser runtime](../../packages/effect-domains/src/browser-runtime.ts))

- `ResourceEditor` mechanically keys its base list and save/remove mutations by the exact Resource descriptor. Successful mutations now settle the editor and trigger subscription refetch without reducer-authored reload commands; manual refresh changes a declared dependency, while cursor continuation remains explicit. Editorial Calendar uses this derived path. ([Resource editor](../../packages/effect-domains/src/resource-editor.ts); [Editorial client](../../examples/editorial-calendar/web/main.ts))
- Field Notes exercises the authored path on the real application page: its query depends on session token, site filter, and manual refresh, while save/remove invalidate `FieldReportsResource`. Identity/filter changes restart the base query; replacing it invalidates stale continuation ownership. Canonical domain and Resource schemas gained no UI, transport, or cache metadata. ([Field Notes client](../../examples/field-notes/web/main.ts))
- The regression runs a mounted Foldkit query and mutation against one native Reactivity service. It observes the initial empty result, performs one successful save, observes the automatically refreshed result, and confirms exactly two reads. Focused ResourceEditor coverage confirms initial loading is subscription-owned and successful mutations issue no reload commands. ([Reactivity regression](../../test/RpcBrowserReactivity.test.ts); [editor regression](../../test/ResourceEditor.test.ts))
- Two rebuilt real browser applications verified the network sequence. Authenticated Field Notes issued `reports.create` for `report_reactivesync1`, immediately followed by an automatic `reports.list`; the persisted SQLite row contained `Automatic reactive synchronization`. Editorial Calendar issued `documents.create`, immediately followed by automatic `documents.list`; SQLite contained `ResourceEditor reactive sync`. No reducer reload command initiated either second request.
- Executable verification passed `bun run check`, `bun run lint`, **139 tests in 30 files** through `bun run test`, `bun run build`, and `bun run docs:build`.

The contract is deliberately browser-local invalidation/refetch over unary `/rpc/v1`. It does not establish server push, cross-tab or cross-client notification, WebSocket/SSE streaming, optimistic writes, offline synchronization, or event-derived invalidation. Those require authored transport and conflict policy rather than mechanical derivation from Resource schemas.

### 2026-09-14: Native Cluster completion and synchronization slices

The durable examples now use one version-sensitive native Cluster boundary with exact Effect release-candidate pins. It selects local single-runner, Bun HTTP runner, or client-only mode and checks persisted topology version, shard count, and groups before starting background work. Report acceptance writes an application-owned execution/outbox row transactionally; a scoped relay dispatches at least once with a deterministic workflow ID. Infrastructure defects suspend for authorized same-execution recovery, cancellation is explicit, and the immutable artifact sink supports missing-file reconciliation while rejecting conflicting content. ([Cluster boundary](../../packages/example-support/src/cluster-runtime.ts); [execution store](../../examples/report-exports/executions.ts); [workflow](../../examples/report-exports/workflow.ts); [artifact writer](../../examples/report-exports/writer.ts))

- Two real report runner processes sharing one execution SQLite file advertised distinct HTTP addresses and split 64 shards 32/32. An approval workflow was retained on shard `default:15`; after its owning runner stopped, the shard moved to the surviving runner. A separate client-only application released the same execution, polling returned `Succeeded`, and the JSON artifact contained the authenticated `admin` release and expected totals. A second automatic export completed through the application outbox/relay, and a separate dispatched export returned `Cancelled`.
- The client-only server originally stalled before listening because the outbox polling loop ran during Layer acquisition. The relay now forks in the Layer scope, so worker acquisition reaches `Worker ready` and the client-only application reaches its HTTP listener while retaining scoped interruption on shutdown. Runner-only registrations are omitted in client mode.
- Focused regressions cover topology drift and compare-and-set upgrades; pre-dispatch and post-writing cancellation boundaries; repeated, missing, and conflicting artifacts; and a transient workflow defect suspended and resumed through the same native execution. ([Cluster regression](../../test/ClusterRuntime.test.ts); [delivery regression](../../test/ReportExports.test.ts); [workflow regression](../../test/WorkflowApplication.test.ts))
- Field Notes adds separate native integration slices without changing canonical Resource metadata. Typed `NoteEdited` events persist in SQL journals, synchronize between two replicas, expose native conflicts, deduplicate repeat transfer, rebuild the projection from the retained journal, and converge by authored `(revision, replicaId)` maximum. At this checkpoint, a mounted in-process AtomRpc prototype also refetched after same-key mutation. That prototype was removed by the later declarative Foldkit cutover recorded above; the current browser contract is covered by [`RpcBrowserReactivity.test.ts`](../../test/RpcBrowserReactivity.test.ts). ([EventLog slice](../../examples/field-notes/sync.ts); [EventLog regression](../../test/EventLogSync.test.ts))
- Final executable verification passed `bun run check`, `bun run lint`, **139 tests in 30 files** through `bun run test`, `bun run build`, and `bun run docs:build`.

The evidence is bounded to colocated processes sharing SQLite, one immutable filesystem effect, one event shape/projection, and one in-process reactive prototype. It does not establish cross-host SQLite coordination, zero-downtime topology migrations, cross-database transactions, general exactly-once delivery, a synchronization framework, event sourcing for Resources, offline browser synchronization, or production identity. Incompatible topology/native-store changes use a documented maintenance window; same-topology binaries may replace runners one at a time.

### 2026-09-13: Client mechanics consolidation

The follow-up client pass removed remaining duplicated browser mechanics without deriving presentation, business policy, or application-specific refresh decisions. `BrowserModel` now supplies shared notice, field-error, form-failure, and ordinary-failure values used by `ResourceEditor` and the authored clients. Appointment reminders, equipment register, purchased guides, repair workshop, and team tasks moved their list ownership to `ResourcePager`, joining reading list and field notes. Six authenticated parent reducers use `IdentitySession.embed` for child command mapping and one explicit `generationChanged` decision. ([Browser state](../../packages/effect-domains/src/browser-model.ts); [pager](../../packages/effect-domains/src/resource-pager.ts); [identity controller](../../packages/effect-domains/src/identity-session.ts))

- `RpcBrowser.command` now binds request-token args, success/failure message schemas, execution, and total Effect result matching. Purchased guides exercises ordinary authenticated get/list calls, reservations exercises transactional business commands and a shared transition constructor, and support cases exercises authored CRUD-like commands plus business transitions. This is command-envelope reuse, not derivation of business inputs or policies from RPC metadata. ([Browser command adapter](../../packages/effect-domains/src/rpc-browser.ts); [guide client](../../examples/purchased-guides/web/main.ts); [reservation client](../../examples/reservations/web/main.ts); [support client](../../examples/support-cases/web/main.ts))
- Every native example now constructs its `ApplicationBun.run` program and passes it to the re-exported `ApplicationBun.runMain` boundary, removing direct platform imports from all thirteen main files. `StaticSpa.layerHttp` directly validates the one site declaration, so the separate `StaticSpa.site` value was removed. Purchased guides and report exports also dropped platform dependencies that had become unused; editorial calendar and equipment register retain theirs for separate native scripts.
- Browser-entry generation was rejected after the higher-value consolidation: each of the thirteen small entries still explicitly names the model, reducer, view, resource layer, and optional identity/devtools configuration, and `Effect.runSync` is intentionally visible only at that browser application boundary. Package-manifest generation was also rejected because the workspace packages have materially different scripts and native dependencies. Neither reduction would remove domain or transport mechanics.
- The rebuilt support-case page loaded through the new static/native boundaries and completed its three initial same-origin RPC reads. The equipment page started against a fresh SQLite database, completed its initial paged list, and rendered “All asset pages loaded.” This is local startup/read evidence, not mutation, authentication, pagination-continuation, or deployment evidence.
- Final executable verification passed `bun run check`, `bun run lint`, **133 tests in 26 files** through `bun run test`, `bun run build`, and `bun run docs:build`.

### 2026-09-13: Support-case SQLite vertical slice

The thirteenth application models public loopback customer-support case management over customers, agents, cases, and append-only case events. Cases use declared optimistic versions and transitions: open work is triaged, assigned only to an on-duty agent, resolved, and reopened back to triage with its assignment cleared. `support.openCase` and `support.advanceCase` are transactional authored operations that keep the case row and event append atomic. The canonical models contain no persistence, transport, UI, or authorization concerns. ([Domain](../../examples/support-cases/domain.ts); [resources](../../examples/support-cases/resources.ts); [operations](../../examples/support-cases/sqlite.ts))

- The board is the second application projection derived through `SqliteView`: it joins current customer data and optional agent data, supports declared customer/priority/status/assignment filters, an inclusive `openedAt` range, and descending keyset order. Case detail keeps its ordered nested event collection in authored SQL using `Table.project` fragments and `Schema.fromJsonString`; no aggregate, event-history, or relationship-loader framework was added. ([Board](../../examples/support-cases/board.ts); [detail contract](../../examples/support-cases/contracts.ts))
- The in-memory regression exercised open, exact-timestamp board filtering, triage, off-duty assignment rejection, successful assignment, stale-version rejection, resolution, joined detail, and ordered events. A temporary SQLite trigger forced the event insert to abort after the case update; the operation returned `SupportCasesUnavailable`, and a fresh read confirmed status/version rolled back together before a later resolution succeeded. This proves ordinary transaction rollback for the exercised write failure, not process interruption or ambiguous-commit reconciliation. ([Regression](../../test/SupportCases.test.ts))
- A disposable server and real generated CLI created a customer and off-duty agent, opened and triaged a case, rejected assignment with `AgentOffDuty`, patched the agent on duty, assigned, resolved, and read the joined board and nested detail. The rebuilt Foldkit page loaded the persisted board, inspected all four events, and reopened the case; the board and detail both showed triaged version 5, no assignee, and the appended reopen note. The headless tab required explicit lifecycle activation because its `requestAnimationFrame` queue was frozen by the harness; RPC responses and the final rendered screenshot were observed after activation.
- Final workspace verification passed `bun run check`, `bun run lint`, **133 tests in 26 files**, and `bun run build`. `bun run docs:build` also passed after this record was added.

The result is bounded evidence for the existing SQLite seams: a third versioned business domain and second flat joined projection required no framework source changes, while nonmechanical assignment policy, state/event transactions, and nested aggregation remained authored. It is not evidence for production support operations, authentication, SLAs, attachments, event sourcing, general aggregate inference, richer joins, retries, or distributed failure handling.

### 2026-09-12: Client and operation boundaries

An audit of all twelve application frontends and twenty authored operations found repeated transport/startup/static-serving code, identity-session commands mixed with HTML, page/request state machines, repeated operation family invariants, and manual forwarding of a joined-list fragment. That dated cutover introduced APIs now superseded by `Command` and `ReadModel`; its behavior remains represented by the current modules. ([Resource capabilities](../../packages/effect-domains/src/resource.ts); [client modules](../../packages/effect-domains/src/resource-pager.ts); [current commands](../../packages/effect-domains/src/command.ts); [current read models](../../packages/effect-domains/src/read-model.ts))

- Type boundaries rejected constructing `ResourceEditor` from a list-only resource. Pager, command, and read-model regressions now live under their current names. ([Editor type check](../../test/ResourceEditor.types.ts); [pager regression](../../test/ResourcePager.test.ts); [command regression](../../test/Command.test.ts); [read-model regression](../../test/ReadModel.test.ts))
- The real rebuilt editorial-calendar surface loaded through `StaticSpa`, `BrowserRuntime`, and `RpcBrowser`; it created and reloaded an article through the derived editor, with the success notice and row visible. The rebuilt appointment-reminders surface rendered the moved identity-session controller through the retained authored view. This is local browser evidence for startup, assets, same-origin RPC, CRUD, and signed-out identity presentation—not production deployment or IdP evidence.
- Focused verification passed 14 tests across `ResourcePager`, `ResourceEditor`, `Operation`, and `SqliteView`. Final workspace verification passed **132 tests in 25 files**, `bun run check`, `bun run lint`, `bun run build`, and `bun run docs:build`.

### 2026-09-12: Resource editor save ownership

An accepted save reply previously always encoded its returned row over the current editor form. Per-key request ownership still considered that reply current after the user changed the form or selected another row, so a slow save could erase newer local work. `ResourceEditor` now snapshots the submitted form and selection. Success still settles the request and authoritatively reloads the list, but replaces the form only when the snapshot remains current; a failure for a superseded form settles without publishing stale field errors. ([Editor](../../packages/effect-domains/src/resource-editor.ts); [regression](../../test/ResourceEditor.test.ts))

- The reducer regression failed before the fix with the submitted `Before` value replacing `Next edit`. It now covers both late success and late failure: newer values remain, the save leaves pending state, stale field errors stay absent, and successful persistence still schedules the list reload.
- The real editorial-calendar page ran against a disposable SQLite database with RPC POSTs delayed by 700 ms. The update persisted the value submitted at click time and reloaded it into the table; text typed while that request was in flight remained in the form with the success notice visible. This is browser evidence for the public editor's save race, not a claim about offline editing or conflict resolution.
- Final verification: `bun run check`; **129 tests in 24 files**; framework package lint; `bun run build`; and `bun run docs:build`. Workspace lint passed every package and retained the same 11 pre-existing root-test style findings recorded by the preceding verification; none are in the changed source or regression.

### 2026-09-12: Resource editor pending-mutation suppression

`ResourceEditor` previously accepted another save or remove message while the same operation was pending. Request ownership hid the earlier reply but did not cancel its RPC effect, so repeated create messages could execute more than one server mutation. The reducer now rejects repeated save/remove messages until the accepted request settles. This is client-side command suppression, not server idempotency. ([Editor](../../packages/effect-domains/src/resource-editor.ts); [regression](../../test/ResourceEditor.test.ts))

- The reducer regression failed before the fix because a second save produced another `Save` command with a new request token. It now proves that repeated save and remove messages emit no command and leave request ownership unchanged.
- Focused verification passed the three editor regressions, framework package typecheck and lint, and the editorial-calendar consumer typecheck.


### 2026-09-12: Resource cursor contract isolation

Generated resource cursors previously scoped themselves only by table name. Two otherwise identical resource declarations with opposite title ordering could therefore accept the same cursor and reinterpret its last-row values under different comparison semantics. Resource cursor scope now includes the resource name, declared filter and range fields, and complete effective ordering, matching the stricter `SqliteView.list` boundary. ([Resource compiler](../../packages/effect-domains/src/resource.ts); [regression](../../test/ResourceCrud.test.ts))

- The regression creates ascending and descending list declarations over the same physical table. It failed before the fix because the descending list accepted the ascending cursor; it now rejects that cursor while ordinary same-contract continuation still returns the next row.
- A direct in-memory SQLite smoke returned `alpha` then `zulu` through the ascending contract and reported the changed-order cursor as rejected. This establishes resource cursor isolation for changed ordering, not signed or secret cursors; authorization still protects row visibility.
- Final verification: `bun run check`; **128 tests in 24 files**; framework package lint; and `bun run docs:build`. Workspace lint passed every package and retained the same 11 pre-existing root-test style findings recorded by the preceding verification; none are in the changed source or regression.

### 2026-09-11: Contract-derived resource editor

The public editorial-calendar page now uses `ResourceEditor.make` to derive its Foldkit RPC client service, model, messages, CRUD commands, keyset page state, request ownership, stale-result rejection, reversible row/form conversion, and reload-after-mutation behavior from the public `Resource.crud` contract plus an explicit form codec. The page still owns labels, controls, layout, copy, error formatting, and the empty form. Filtered, authenticated, non-CRUD, or alternate-refresh pages remain explicit rather than being forced through this adapter. ([Editor compiler](../../packages/effect-domains/src/resource-editor.ts); [form codecs](../../packages/effect-domains/src/form.ts); [request ownership](../../packages/effect-domains/src/requests.ts); [editorial page](../../examples/editorial-calendar/web/main.ts))

- The shared form codecs and request tracker moved from example-web into the framework, and every example caller cut over to the framework exports. The old example-web modules and exports were removed.
- A reducer regression covers initial list dispatch, stale list rejection, accepted paging state, row-to-form encoding, field changes, save dispatch, mutation settlement, and reload. During live browser verification, whole-form input messages exposed a lost-update race when rapid inputs captured an old rendered form. The editor now sends field deltas and merges them against the current model; the browser then submitted the exact heading, summary, priority, channel, and publication timestamp.
- A disposable SQLite server and the real page exercised empty-form validation, inline field errors, create, update, post-mutation reload, and a fresh read. The final row was `Release calendar verified`, priority `3`, channel `newsletter`, with `2026-10-02T09:00:00.000Z`; the page had no captured errors. This is local public-CRUD browser evidence, not evidence for authentication, filters, bulk edits, optimistic writes, arbitrary RPC groups, or generated presentation.
- Final verification: `bun run check`; **127 tests in 24 files**; framework package lint; `bun run build`; and `bun run docs:build`. Workspace lint passed every package and retained 11 pre-existing root-test style findings in `Application.test.ts`, `Authorization.test.ts`, `RelationalMigrations.test.ts`, `Reservations.test.ts`, `ResourceCrud.types.ts`, `RpcService.test.ts`, and `SqliteView.test.ts`; none involve the editor change.

### 2026-09-11: Abstraction pass

**Current cutover; final workspace validation passed.** An inventory of the twelve applications found the same mechanical code repeated where the framework offered nothing: sixteen native handlers with the `requireSubject → SqlClient → withTransaction → catchTags` skeleton and five `persistenceFailures` maps; hand-written `UPDATE … WHERE version = ? AND status = ?` guards; two shapes of the same `{ from, to }` transition table and three `{ id, action, actual }` error classes; hand-built range/`ORDER BY`/limit list queries; five insert-if-absent seeds in four idioms; two identical entitlement resolvers; an identity store with its own migration ledger; ~4,900 lines of migration JSON serializing every snapshot two or three times; and re-authored UUIDv7, safe-integer, page-limit, and timestamp schemas. The pass derived each of those and left business rules authored. ([Resource](../../packages/effect-domains/src/resource.ts); [Transitions](../../packages/effect-domains/src/transitions.ts); [Operation](../../packages/effect-domains/src/operation.ts); [Entitlements](../../packages/effect-domains/src/entitlements.ts); [SQLite identity](../../packages/effect-domains/src/sqlite-identity.ts); [Migrations](../../packages/effect-domains/src/sqlite-migrations.ts); [Domain values](../../packages/effect-domains/src/domain.ts))

- Billing: `orders-invoices/sqlite.ts` fell from 498 to 266 lines. Orders and invoices declare `version` and `Transitions`; five operations use `Operation.make` with the shared editor policy and transactions; the duplicate-number pre-selects are gone and `UniqueViolation` is translated to `DuplicateOrderNumber`/`DuplicateInvoiceNumber`; foreign keys use `scope: ["tenantId"]` with derived names; the nested summary uses `Table.project`. Live CLI on a fresh database: create, duplicate number rejected, add line (version 2), stale `expectedVersion: 1` → framework `VersionConflict`, issue invoice (order `invoiced`, version 3), pay, second pay → `InvalidInvoiceTransition { action: "pay", actual: "paid" }`, reader `Forbidden`, generated `orders.list` with a status filter. The prebuilt admin rendered the inspection, authenticated with a bearer token, and listed orders with filters and paging.
- Reservations declare `Transitions` and run `confirm`/`release` as transactional operations that still author stock restoration; live CLI: reserve → confirm → release rejected with `InvalidReservationTransition`, stock consistent. Expense ledger declares `range: ["date"]`, `order: [["date", "asc"]]`: live keyset paging across a cursor boundary within a date range and a category filter, plus the authored totals aggregate. Repair workshop declares urgent-first ordering with the `SqliteView` board as an operation.
- Purchased guides and report exports replaced their resolver services with `Entitlements.fromTable` sources (grant predicates authored); team tasks, field notes, and both durable applications reuse the shared `ExampleRoles` ladder and `sameAs` tenant scopes; seeds use `repository.ensure`; the six protected applications use `SqliteIdentity` through `ExampleIdentity.layer(application)` with per-application `<APP>_IDENTITY_DB` databases, and the durable applications open their execution stores through `SqliteBunRuntime.privateClient`. Example-support shrank to a subject schema, roles, the identity wrapper, and atomic file replacement.
- Migration artifacts moved to v2 by a one-off converter and total 2,665 JSON lines (from 4,938). Single-artifact histories whose physical snapshot changed (billing, workshop, report subscriptions) were regenerated; multi-artifact histories are unchanged and still verify against their ledgers.
- Final verification: `bun run check` clean, `bun run lint` **0 findings** (baseline 5), `bun run test` **120 tests in 22 files**, `bun run docs:build`. New regressions cover keyset paging with a descending field, guarded updates and `VersionConflict`, `UniqueViolation` constraint naming, transition rejection and publication, `ensure` idempotence, implicit null defaults, derived and scoped relations, v2 history chaining and implicit copies, operation failure translation/policy/rollback, table-backed entitlements, `sameAs`, and subject-policy embedding.
- Limits: the framework grew from 5,269 to 7,193 source lines, partly from the project's one-statement-per-line lint style; that cost is visible and not yet offset by more domains. `Operation.make` translates every undeclared failure to the `unavailable` error after logging, so a handler that forgets to declare an error hides it from clients. Transitions and versions are exercised by two domains, range/order by two; the admin was verified only for lists and forms, not for versioned patch or transition submission. Public applications (reading list, editorial calendar, equipment register, reservations, expense ledger, repair workshop) still start without identity configuration.

**Follow-up, same day.** `Operation.make` now rejects at compile time any domain-tagged failure the handler can raise but the contract omits (infrastructure and untagged failures still collapse to `unavailable`), and protected operations pass nested `Forbidden`/entitlement failures through instead of hiding them; a type regression and a behaviour regression cover both. Transition errors carry a rendered `message`. `RpcBundle.make(group)(handlers)` replaces the ad-hoc bundle construction. Repair workshop declares `RepairJobTransitions` (`start`, `finish`) with `operations.transition: true`; the prebuilt admin rendered its `Repair Jobs Transition` form with a literal `action` select, a valid `start` returned the `repairing` row and a repeat rendered `InvalidRepairJobTransition`, and MCP `tools/list` exposed `repair_jobs.transition` with `action.enum`. A team-tasks database created at the previous commit failed current startup with `MigrationError: migration history changed at 001_initial`; the three multi-artifact READMEs now say so. `ReportExport.Generate` had been passing the native workflow's `execute` unbound through `pipe` since the examples wave and failed at runtime; it now calls the method and returned the artifact object live, while `GenerateDiscard`, `Poll`, and `Release` behaved as documented. A behaviour-preserving split of `resource.ts` into list/write modules was attempted and reverted because it widened repository requirement types; the file stays whole. Ten user-facing docs pages were re-read against source and corrected (one-record `Operation.make`, required `success`, `ApplicationBun.run` entrypoints, `new Entitlements.Source`, version-aware transition payloads). Decisions: nested aggregation stays authored through `Table.project` because `SqliteView` is a flat row projection and `json_group_array` is not; CHECK constraints stay part of drift verification because they are real SQLite constraints, so check-only rebuilds remain history. ([Operation](../../packages/effect-domains/src/operation.ts); [operation regressions](../../test/Operation.test.ts); [type regression](../../test/Operation.types.ts); [workshop machine](../../examples/repair-workshop/domain.ts); [report workflow](../../examples/report-exports/workflow.ts))

### 2026-09-11: Native identity and browser client cutover

The current cutover separates portable `effect-domains/identity` schemas and narrow runtime service from the native `effect-domains/identity-rpc` group/handlers. The example identity runtime used a separate SQLite store, issued real opaque credentials, authenticated a current session on each protected call, and revoked it through `identity.logout`. It persists SHA-256 credential digests, applies expiry/revocation and current disabled-account/claim checks, and uses Argon2id password verification. This is bounded example-store evidence, not production IdP or user-management evidence. ([Portable interface](../../packages/effect-domains/src/identity.ts); [native RPC](../../packages/effect-domains/src/identity-rpc.ts); [example store](../../packages/example-support/src/identity.ts))

- All six protected applications composed the native identity group and handlers. Real CLI login returned an issued token, ISO expiry, and subject; current returned expiry/subject, and logout revoked the credential. The same credential and expiry remained valid after server restart until logout. No accepted static bearer strings remained.
- The official MCP SDK exercised identity login/current/logout and Bob's entitled guide read. Current/logout used their published `{ "input": null }` contract; a revoked credential failed the subsequent current call. Live account disabling and expiry changes rejected an existing credential, while updated account roles appeared on its next current call. Stored credentials matched SHA-256 digests and password hashes used Argon2id.
- The twelve real Foldkit surfaces used scoped native RPC clients. Reading-list CRUD, list append from 25 to 32 rows, nullable note/rating behavior, team-task false/true filtering, field-note plaintext retrieval through its storage codec, billing validation/total/versions, and the documented editorial, equipment, workshop, expense, reservation, report, and reminder flows were exercised. Request regressions covered replaced errors, identity epoch reset, key-only invalidation, and independent writes. ([Browser protocol](../../packages/example-web/src/rpc.ts); [request helper](../../packages/effect-domains/src/requests.ts); [browser regressions](../../test/ExampleWeb.test.ts))
- A reading-list filter change replaced the 32-row accumulated page with 16 matching rows and cleared its cursor. An older intercepted query failed after the replacement response arrived; its error was ignored and the current rows remained visible.
- A held authorized guide response released after logout was ignored. Password fields cleared immediately; browser local and session storage were empty. Report session switches cleared draft/execution state, and reminder logout cleared recipient/location/purpose while regenerating IDs and dates. Roles remained server-enforced: a valid Alice session was forbidden to schedule an admin-only reminder.
- Final verification passed **102 tests in 22 files**, `bun run check`, `bun run lint`, `bun run build`, and `bun run docs:build`. An earlier concurrent run hit two five-second test deadlines while builds and twelve smoke servers were active; the final suite passed after the owned runtimes stopped, without timeout changes.

The earlier [standalone-guide browser observation](#2026-09-11-standalone-example-guides) is retained as a historical checkpoint for that revision; it does not describe the corrected current browser clients.

### 2026-09-11: Standalone example guides

At that checkpoint, every application had its own README, linked from the [example index](../../examples/README.md) and [documentation catalogue](../examples.md). The documentation smoke used disposable databases, separate ports, and the real generated Bun CLI across all twelve applications. It is retained for its documented CLI and route observations; its browser limitation below is not a claim about the corrected current clients.

- Reading-list CRUD, equality filters, cursor traversal, and rating rejection passed; expense inclusive-date queries, currency-local totals, correction/removal, and typed errors passed; the equipment application's official MCP SDK lifecycle and equivalent CLI flow passed. ([Reading list](../../examples/reading-list/README.md); [expense ledger](../../examples/expense-ledger/README.md); [equipment register](../../examples/equipment-register/README.md))
- Team-task subject bindings, visibility, cursor pages, completion locks, and admin reopen/transfer rejection passed. Field-note plaintext reads through encrypted storage, global tenant visibility, reader mutation denial, full updates, and admin removal passed. Orders reached an issued and then paid invoice with the documented totals/versions; stale writes, illegal transitions, reader mutation, and cross-tenant reads failed as documented. Purchased guides distinguished unlocked, locked, hidden, and whole-page entitlement results. ([Tasks](../../examples/team-tasks/README.md); [notes](../../examples/field-notes/README.md); [billing](../../examples/orders-invoices/README.md); [guides](../../examples/purchased-guides/README.md))
- The actual editorial v1 seed upgraded with the documented rename and backfills. The repair board returned current names, booleans, missing-technician nulls, filters, and bounds; foreign-key violations failed. Reservation reserve/confirm/release preserved the documented stock accounting and rejected invalid inputs/transitions. Restart reads preserved the reading-list row, upgraded editorial row, encrypted note under the same key, joined repair changes, and reservation count/state. ([Editorial calendar](../../examples/editorial-calendar/README.md); [repair workshop](../../examples/repair-workshop/README.md); [reservations](../../examples/reservations/README.md))
- Approval-gated and automatic reports produced real JSON artifacts with the documented totals. Generation rejected a reader and an unpaid account. `Generate` returned the result directly; `Poll` wrapped it with a state tag. Reminder acceptance preceded delivery; the delivered row matched its JSON projection, repeated submission did not duplicate it, and a future reminder was delivered after switching to worker-only execution. Both standalone workers opened no HTTP listener. ([Reports](../../examples/report-exports/README.md); [reminders](../../examples/appointment-reminders/README.md))
- Reminder mismatch/time-order errors were observed through the blocking `ScheduleReminder` operation using fresh IDs and already-due times. The discard operation acknowledges acceptance without returning eventual handler failures. Authenticated non-admin scheduling failed with `Forbidden`, while their inbox list returned an empty page and direct get returned `ResourceNotFound`. The guide now distinguishes these outcomes. ([Entity contract](../../examples/appointment-reminders/appointment-reminder-entity.ts); [handler checks](../../examples/appointment-reminders/background.ts); [visibility policy](../../examples/appointment-reminders/resources.ts))
- `bun run build` and `bun run docs:build` passed. Markdown link targets/heading anchors and shell/JSON snippet syntax were checked. All twelve frontend routes returned HTTP 200; enabled admin routes returned 200 and the other four returned 404. The documentation catalogue was visually checked in Chromium. No executable source was changed for this documentation task; no project-wide typecheck, lint, or test suite was run.

Two runtime limitations were observed rather than covered up by broader success claims:

- The reading-list Foldkit page rendered its controls but remained **Loading…** during the headless Chromium check. Its `/rpc/v1/` POST was observed; no page exception was captured. The CLI succeeded, but this run did not establish working browser mutations or diagnose the loading behavior.
- Stopping report serve immediately after an automatic `GenerateDiscard` acknowledgement, then starting its worker against the same stores/output path, left that execution without an artifact. After returning to serve, `Poll` reported `Failed` with `EntityNotAssignedToRunner`; an explicit `GenerateResume` followed by `Poll` still reported failure. This is a counterexample to assuming recovery at every interruption point, not a failure of the separately exercised uninterrupted report flows. The [report guide](../../examples/report-exports/README.md#web-mcp-and-operations) records the caveat. Native interruption recovery remains an open investigation.

### 2026-09-11: Repair-workshop joined projections

The repair workshop was implemented and run before adding framework support: its original native `SqlSchema` handler manually selected aliased customer/technician fields and duplicated Boolean storage decoding. The then-named `SqliteView` derived that projection; its clean-cutover successor is `ReadModel`. ([Example](../../examples/repair-workshop/README.md); [declaration](../../examples/repair-workshop/board.ts); [publication](../../examples/repair-workshop/sqlite.ts); [current compiler](../../packages/effect-domains/src/read-model.ts))

- The original server created customers, technicians, an assigned urgent repair, and an unassigned ordinary repair through the real CLI. After the cutover and restart, its board returned exactly the same serialized rows.
- Live CLI edits changed customer/technician names and the on-call flag; subsequent joined reads returned current values. Assignment, unassignment, status filtering, and a one-row bound worked. Invalid foreign keys, deletion of a referenced technician, and a limit above 100 failed; the failed writes left the board intact. Local inspection exposed the complete join/dependency declaration.
- The official MCP SDK discovered and invoked `workshop.board`; structured results retained canonical `true`/`false` and unmatched-row `null`, rather than SQLite bits.
- The actual Foldkit page loaded persisted rows. Scripted DOM input/change/click events created a customer, technician, and repair, edited the customer, changed repair status, removed an assignment, and filtered/reloaded the board. Screenshots confirmed the full-width joined board and forms; real CLI reads confirmed the resulting rows. Native browser input helpers stalled in the harness, so event dispatch was used instead of claiming keyboard/mouse verification.
- All **95 tests in 19 files** passed at that checkpoint. The current regressions retain joined codec, graph, dependency, and type-boundary coverage. ([Regressions](../../test/ReadModel.test.ts); [type boundaries](../../test/ReadModel.types.ts))
- Workspace typechecks, unchanged workspace lint plus the root lint pass, and the admin/Foldkit build passed.

This is a public loopback record-keeping example, not production identity, tenant policy, repair scheduling, billing, guarded business transitions, load testing, general aggregate inference, automatic authorization of joined SQL, or distributed cache coherence. Join equality is explicitly physical equality; ambiguous stored-null transformations require an authored discriminator and result codec.

`bun run docs:build` passed. The owned smoke server and browser tabs were closed; the temporary SQLite database and migration-authoring script were removed.

### 2026-09-11: Automatic RPC telemetry

The Bun runtime now supplies a scoped native Effect OTLP tracer automatically when a traces endpoint is configured. Declarative runtime options configure export without changing domain schemas, operation contracts, handlers, or authorization. The exported `ApplicationTelemetry.layer` also supports custom runtimes with a runtime-provided native HTTP client. ([Runtime](../../packages/effect-domains/src/application-bun.ts); [telemetry layer](../../packages/effect-domains/src/application-telemetry.ts); [configuration reference](../reference/runtime.md#opentelemetry-tracing))

- A live local HTTP collector received spans from a Bun application combining reading-list generated RPCs and an authored success/failure RPC. CLI and server spans shared trace IDs; generated `books.list` succeeded, and declared failures produced error span status without changing the RPC error response.
- Admin and MCP HTTP requests with distinct W3C trace parents reached handlers with the corresponding trace IDs. Each exported in-process server span had its own matching client parent, rather than a captured startup/request span.
- Explicit endpoint, JSON protocol, service identity/version/attributes, collector authorization headers, and shutdown timeout overrode conflicting deployment values. Native protobuf export produced an OTLP protobuf request at `/v1/traces`. Environment resource attributes supplied service identity/version when explicit options were absent.
- A one-hour export interval still flushed CLI spans at command completion and worker spans at graceful termination. Explicit opt-out, SDK disablement, exporter `none`, and no-endpoint operation preserved successful RPC responses without exporting client traces.
- Workspace typechecks and all **90 existing tests in 18 files** passed. Framework package lint passed. Workspace lint remained failing in unrelated `packages/example-web` and `examples/reading-list` code.

These are local transport/export observations, not a production collector interoperability certification, browser visual test, load/memory bound, collector-outage recovery proof, crash-safe delivery guarantee, durable trace persistence, or a new sampling/metrics/logging implementation. The temporary collector and runtime probes were removed after verification.

### 2026-09-10: Entitlement gating

Resource and subject policies declare string-keyed entitlement requirements; an application-provided Effect service resolves current access. `EntitlementRequired` distinguishes a missing grant from `EntitlementUnavailable`. Generated repositories, native RPC middleware, authored authorization calls, and inspection share that contract. No provider SDK, charging, checkout, or quota consumption was added. ([Authorization](../../packages/effect-domains/src/authorization.ts); [resolver interface](../../packages/effect-domains/src/entitlements.ts); [adapters](../../packages/effect-domains/src/resource.ts))

- Workspace `bun run check`, unchanged `bun run lint`, and all **90 tests in 18 files** passed. Six entitlement regressions exercise empty-list denial, missing/failing resolvers, grant/revoke freshness, hidden rows, cursor lookahead and whole-page failure, transactional rollback after a database trigger revokes a grant, descriptor snapshots, standalone guards, and concurrent request-local RPC identity. Compile-only probes reject invalid key types and unavailable phases. ([Regressions](../../test/Entitlements.test.ts); [type boundaries](../../test/Entitlements.types.ts))
- The purchased-guides server applied its initial migration. Real CLI calls returned a purchased guide, `EntitlementRequired` for a visible unpurchased guide and mixed list, and `ResourceNotFound` for another tenant's guide. Updating the persisted purchase to `refunded` denied the previously accessible guide; restart did not restore its grant.
- The report server applied its subscription migration. An editor on a paid account submitted full report data; an unpaid account received `EntitlementRequired`, and a reader received `Forbidden`. Native workflow/queue execution wrote a real financial JSON artifact with the expected totals. Expiry denied subsequent submissions while admin polling remained available; restart did not renew the subscription.
- Explicit cancellation grace restored access. After granting a second account, identical report IDs produced distinct account-bound execution IDs and separate completed artifacts. Local inspection exposed both subject-bound report requirements and row-bound guide requirements. Operator metrics returned HTTP 401 anonymously, 403 to a reader, and 200 to an admin. ([Report implementation](../../examples/report-exports/workflow.ts); [subscriptions](../../examples/report-exports/subscriptions.ts); [guide grants](../../examples/purchased-guides/entitlements.ts))
- `bun run docs:build` passed. Owned smoke servers were stopped, and their temporary databases and generated artifacts were removed.

These observations establish local SQLite/CLI/RPC behavior, not production authentication, external payment processing, webhook reconciliation, atomicity with a billing provider, metered consumption, browser interaction, new live MCP coverage, or forced-restart recovery of in-flight account-bound workflows. Accepted work is not reauthorized on every durable step. Report workflow payloads now include trusted account identity; old unscoped execution payloads are not migrated.


### 2026-09-10: Shared example abstractions

The implementation extracts a canonical Gregorian date-only schema and subject-only policies over the existing authorization AST. Expense CRUD reuses its resource repository while preserving authored names/errors and deleted-row removal; native SQL fragments and schema JSON codecs remove repeated expense and billing machinery. Durable examples share private SQLite composition and scoped atomic file replacement without a framework execution facade. The user explicitly approved retiring `schema-record-interface`, which required the unused interfaces being removed; the other lint rules remain enabled through one shared root configuration. ([Domain values](../../packages/effect-domains/src/domain.ts); [authorization](../../packages/effect-domains/src/authorization.ts); [expense handlers](../../examples/expense-ledger/sqlite.ts); [billing handlers](../../examples/orders-invoices/sqlite.ts); [support](../../packages/example-support/src/); [lint configuration](../../better-typescript.json))

- `bun run check`, `bun run lint`, and all **84 tests in 17 files** passed. Added regressions exercise a shared policy across generated reads and authored writes, request-local authentication, no cross-RPC policy leakage, frozen allow-list semantics, malformed claims, and rejection of row-dependent subject policies. Existing execution regressions now exercise `privateSqlite` directly, including symlink/hardlink rejection and in-memory separation. ([RPC regressions](../../test/AuthorizationRpc.test.ts); [policy regressions](../../test/Authorization.test.ts); [type boundaries](../../test/Authorization.types.ts); [execution isolation](../../test/ApplicationExecution.test.ts))
- Real expense CLI-over-HTTP calls exercised create/get/update, deleted-row removal, missing get/update errors, both optional-category branches, separate currency totals unaffected by the query limit, reversed ranges, and invalid calendar dates.
- Real billing CLI calls exercised reader mutation denial, empty/populated line projections, nullable/populated invoice projections, invoice issuance/payment, stale-version rejection, and tenant isolation.
- The reminder server rejected editor and anonymous proxy submissions before acceptance. Repeated admin submission delivered one inbox row; the same policy hid inbox rows from editors. Local inspection exposed the proxy's subject schema and rendered admin rule.
- A native report workflow and queue completed and published a real JSON artifact. The reminder singleton published its notification projection through the same atomic replacement helper. A separate filesystem probe replaced an existing file and verified temporary-file cleanup after a rename failure.
- `bun run docs:build` passed. Owned smoke servers were stopped and their temporary databases and output files removed.

These checks establish the listed local contracts, not new browser interaction, live MCP client behavior, forced-restart durable recovery, multi-runner execution, crash-durable file flushing, or cross-database transactions. Existing migration artifacts and business transition logic were intentionally unchanged.


### 2026-09-10: Domain-first example applications

Eight feature fixtures were replaced with reading lists, an expense ledger, team tasks, encrypted field notes, an editorial calendar, an equipment register, financial report exports, and appointment reminders. Reservations and orders/invoices retain their business behavior. Each application owns its canonical model; the unused shared book fixture was removed. Task and editorial migrations retain their historical physical table names because the current migration API does not rename tables. ([Application guide](../../examples/README.md); [task history](../../examples/team-tasks/migrations.ts); [editorial history](../../examples/editorial-calendar/migrations.ts))

- `bun run check`, unchanged `bun run lint`, all **82 tests in 17 files**, the admin build, and the documentation build passed.
- All ten applications started against isolated SQLite databases. Real CLI calls exercised reading progress and ratings, currency-separated expense totals and bounded date queries, task completion locks and tenant isolation, role-protected field reports, editorial creation, reservation transitions, and order/invoice payment.
- The editorial version-one seed upgraded through normal server startup: its heading survived, priority became 0, channel became website, and publication time remained null. Replacement domains use fresh histories; they do not silently import retired demo databases.
- The official MCP SDK walkthrough registered, moved, filtered, retired, and removed an equipment record, then observed the declared missing-record error. The reading-list browser admin displayed the persisted book and declared filters; no admin implementation changed.
- Field-report CLI roundtrips preserved NUL and a lone UTF-16 surrogate while SQLite stored an encrypted envelope. Regression coverage rejects wrong keys and tampered ciphertext with `RepositoryError`, keeping encryption independent from role authorization. ([Codec](../../examples/field-notes/storage.ts); [regression](../../test/ResourceCrud.test.ts))
- An approval-required report remained pending, survived process termination, accepted release after restart, and wrote an actual JSON artifact with currency-local debit/credit totals and the authenticated releasing operator. Inverted reporting periods were rejected at the input boundary. ([Workflow](../../examples/report-exports/workflow.ts); [period regression](../../test/WorkflowApplication.test.ts))
- Appointment notification delivery was deduplicated; recipient mismatches and reminders at the appointment time were rejected. A future accepted reminder survived process termination and was delivered by a standalone worker; its singleton projection contained the recovered notification and the worker had no HTTP listener. ([Entity](../../examples/appointment-reminders/appointment-reminder-entity.ts); [background](../../examples/appointment-reminders/background.ts))

These checks establish the listed local workflows, not production identity, payment processing, external email/SMS delivery, full-database encryption, automatic key rotation, accounting compliance, or multi-runner recovery. Historical records below describe their original fixtures even when source links point to successor applications. Owned smoke processes, browser handles, and temporary artifacts were cleaned up.

### 2026-09-10: JSON CLI and imported migration history

**Current cutover; final workspace validation passed.** The approved A+B refactor removes generated field flags, manifest files, `SqliteMigrations.load`, and `database.manifest`. CLI operations use canonical `--input-json`; native application subcommands, help, and local inspection remain. Applications import frozen artifact JSONs in order, decode them with `SqliteMigrations.decodeHistory`, and pass `database: { migrations, filename? }`. Artifact contents and formats, immutable ledger checks, exact schema/index drift checks, foreign-key verification, and transactional replay remain. ([CLI](../../packages/effect-domains/src/rpc-cli.ts); [Bun runtime](../../packages/effect-domains/src/application-bun.ts); [migrations](../../packages/effect-domains/src/sqlite-migrations.ts))

- Framework source decreased from **5,320 to 4,521 lines: 799 fewer lines (15.0%)**, counting all `packages/effect-domains/src/*.ts`, including the new private algebra module. This is a separate reduction from the preceding cutover, not a cumulative percentage. No framework code was relocated to claim this result.
- Table storage and authorization scalar analysis used distinct algebras over one cycle-aware schema fold. The current clean-cutover module is `SchemaField`, and its regression retains layer-map, AST-evidence, codec, collection, cycle, and copy-isolation coverage. Creation input, evaluation, and inspection now share the Resource creation fold. ([Field fold](../../packages/effect-domains/src/schema-field.ts); [fold tests](../../test/SchemaField.test.ts); [resource tests](../../test/ResourceCrud.test.ts))
- Migration steps and copy expressions compile exhaustively to native SQL statements. Expected/actual catalog comparison replaces separate inspection pipelines. Independent review caught an intermediate dotted-identifier quoting regression; whole-name quoting was restored. Regressions cover dotted names, literal backfill values, missing indexes, untracked triggers, historical replay, and rollback. Frozen artifact JSONs were not rewritten. ([Migration compiler](../../packages/effect-domains/src/sqlite-migrations.ts); [migration regressions](../../test/SqliteMigrations.test.ts); [relational regressions](../../test/RelationalMigrations.test.ts))
- Final `bun run check`, `bun run lint`, `bun run test`, `bun run build`, `bun run docs:build`, and `git diff --check` all passed. The test run passed **81 tests in 17 files**. Lint rules were not relaxed. All 56 current shell JSON payload examples parsed; the documented migration-authoring sample constructed and decoded its two artifacts without database replay.
- Live basic CRUD used the actual JSON CLI and a temporary SQLite database. Create, get, bounded list, update, remove, and empty list succeeded. Restart preserved the updated row and accepted the imported migration history. Removed native flags and a missing required payload field failed. Local `inspect books.create` worked with an invalid remote URL. Admin HTML and metadata returned HTTP 200. The temporary server was stopped afterward. ([Basic application](../../examples/reading-list/main.ts); [imported history](../../examples/reading-list/migrations.ts); [CLI regressions](../../test/RpcCli.test.ts))

These observations do not establish a new live MCP/browser-interaction, historical-upgrade, or durable-worker restart run for this revision. Those boundaries have regression coverage and earlier dated evidence, not new live verification here. Dated observations of field flags and manifests remain historical; they do not describe current commands.

### 2026-09-10: Radical simplification

**Preceding cutover; final workspace validation passed.** This section supersedes earlier API descriptions of inferred migration planning/generation and rename/backfill/transform intents, the schema CLI, `ApplicationBun.execution`, configurable list ordering, unbounded generated lists, and the separate repository `page` method. Earlier dated observations remain unchanged and apply to their original revisions only.

The approved boundary retains explicit `SqliteMigrations.make({ id, from, to, steps })`, fresh `initial({ id, tables })`, snapshots, loading/decoding, frozen artifact JSON, the applied-artifact ledger, and exact schema/FK/transaction verification. Native execution composition moves to application `services`; the example helper checks opened file paths and inodes before native execution tables initialize, not before opening the files. Generated lists always return bounded `{ items, nextCursor }`, default to 50 items, use declared equality filters, and order only by identifier ascending. Resource CRUD/patch and native worker/background/routes/initialization remain. ([Migrations](../../packages/effect-domains/src/sqlite-migrations.ts); [runtime](../../packages/effect-domains/src/application-bun.ts); [database helper](../../packages/effect-domains/src/sqlite-bun.ts); [Resource](../../packages/effect-domains/src/resource.ts))

The implementation consolidates supported field compilation, a private creation-source language (`Input`, `Default`, `Generated`, `Subject`), closed resource operation descriptors consumed by native RPC/repository interpreters, and the existing policy algebra with private compiled authorization registration. These are bounded internal languages, not a general schema framework or a second transport model. ([Table](../../packages/effect-domains/src/table.ts); [Resource](../../packages/effect-domains/src/resource.ts); [Authorization](../../packages/effect-domains/src/authorization.ts))

Documentation checks completed during the cutover: `bun run docs:build` passed, and the exact [migration-authoring example](../../examples/README.md#review-schema-changes) ran under Bun, emitted JSON, and passed `decodeHistory`. This checked authoring and decoding only, not database replay. A repository-relative link-target scan found no missing targets. Temporary authoring code was removed.

Final `bun run check` and `bun run lint` passed across every workspace and the root, with the existing lint rules unchanged and no suppressions. `bun run test` passed all 68 tests across 16 files; `bun run build` passed. Runtime-only carriers use native Effect data constructors rather than opaque schema wrappers. The framework source decreased from 7,019 to 5,320 lines, including the new creation DSL module: **1,699 lines removed (24.2%)**. This is a measured source reduction, not a complexity metric. ([Creation DSL](../../packages/effect-domains/src/resource-creation.ts); [resource regressions](../../test/ResourceCrud.test.ts); [type contracts](../../test/ResourceCrud.types.ts))

A 75-case old/new differential probe found identical table metadata and ordering capabilities across supported scalar, nullable, suspended, checked, and codec forms. Nine table regressions include direct database numeric/enum checks, historical DDL check forms, and database-generated UUIDv7 identity. Existing JSON migration artifacts remain byte-for-byte unchanged. A direct Bun run seeded the historical document schema, then replayed the complete history against the current compiled Resource: the document retained its identifier and returned `heading: "Migration guide"`, `summary: null`, and `priority: 0`. ([Table regressions](../../test/Table.test.ts); [migration regressions](../../test/SqliteMigrations.test.ts); [seed](../../examples/editorial-calendar/seed-v1.ts))

Live loopback checks used isolated temporary databases. Generated CLI create/get and two-page identifier cursor traversal passed. The official MCP SDK client discovered and exercised book create/get/update/list/remove and received the expected missing-row error afterward. The admin page and metadata loaded, and same-origin admin HTTP calls created a book and returned a paginated list. Both durable example workers reached `Worker ready` using application-owned private native SQL layers. All owned smoke servers/workers were stopped. The three execution regressions verify private SQL context isolation, example-owned symlink/hardlink rejection before execution initialization, and separate in-memory connections. ([MCP client](../../examples/equipment-register/client.ts); [admin adapter](../../packages/effect-domains/src/application-admin.ts); [execution regressions](../../test/ApplicationExecution.test.ts))

These checks do not repeat interactive browser navigation, forced-restart durable recovery, packed-package consumption, or multi-runner behavior. The historical guarantees about rejecting database paths before either file opens are retired; the new example checks run after the connections open but before execution storage initializes. Earlier records retain their original evidence and limits.

### 2026-09-10: Schema-native interpreter refactor

Canonical schemas and native RPCs remain authoritative. One compiled field projection supplies storage and column metadata; the closed policy fold supplies evaluation, SQL, validation, and snapshots; repository calls share identity/access/transaction setup; migration planning reconciles schema-backed field and table changes. Adapter normalization returns a native unary RPC instead of a separate procedure DTO. Public resource/application composition, explicit business policy, and semantic storage codecs remain intact. ([Table](../../packages/effect-domains/src/table.ts); [Policy](../../packages/effect-domains/src/policy.ts); [Resource](../../packages/effect-domains/src/resource.ts); [Migrations](../../packages/effect-domains/src/sqlite-migrations.ts); [RPC contract](../../packages/effect-domains/src/rpc-contract.ts))

`bun run check` and `bun run lint` passed across the workspace and root, with every existing custom lint rule retained and no suppressions. All 58 tests across 16 files passed. Existing negative type probes caught a variadic-wrapper inference regression that had widened repository parameters to `any[]`; a mutable tuple view at the native `Effect.fn` boundary preserves the original schema-derived inputs without copying runtime arguments. The pagination regression now exercises mixed Boolean/string order values through the schema-derived cursor tuple. ([Type contracts](../../test/ResourceCrud.types.ts); [subject-bound type contracts](../../test/Authorization.types.ts); [resource regressions](../../test/ResourceCrud.test.ts))

Final live loopback checks exercised authenticated CLI creation and patching, cross-subject list isolation, browser-admin creation and visible persisted rows, and official-SDK MCP discovery/create/get/update/list/remove with the declared missing-row error. The migration application restarted against its existing upgraded database and returned the retained `Migration guide` with `summary: null` and `priority: 0`. Restarts exposed an incidental DDL change from singleton `=` checks to `IN`; restoring the original predicates removed false schema drift without weakening verification or resetting the databases. Owned browser tabs and smoke servers were closed. ([CRUD walkthrough](../../examples/team-tasks/README.md); [MCP client](../../examples/equipment-register/client.ts); [migration walkthrough](../../examples/editorial-calendar/README.md); [DDL](../../packages/effect-domains/src/sqlite-ddl.ts))

The framework footprint changed from 6,903 lines / 286,991 bytes to 7,018 lines / 289,375 bytes: 115 lines and 2,384 bytes added. Shared execution and representation ownership improved, but preserving the complete lint policy required additional named adapters and schema companions; this is not a textual compaction result. These checks do not repeat durable-workflow recovery, packed-package consumption, or multi-runner verification and do not establish general equivalence beyond the exercised contracts.

### 2026-09-10: Declarative composition verification

The clean-cutover declarative API was verified without compatibility adapters: resource publication/configuration used one `operations` record, `Resource.crud`, configured private create defaults, subject-bound CRUD, and explicit patch; a composed application used nested resources and a native command through `Application.make({ name, parts })`. The scenario confirmed local repository policy can remain private while selected RPC operations are published, tenant visibility hides rows, and authorization denials remain enforced. ([Resource](../../packages/effect-domains/src/resource.ts); [Application](../../packages/effect-domains/src/application.ts); [Authorization](../../packages/effect-domains/src/authorization.ts))

All 58 Vitest tests across 16 files, typechecks, lint, the admin-assets build, and the documentation build passed. A live composed custom application CLI exercised the nested resource/native-command composition, private create defaults, subject-bound CRUD, patching, tenant hiding, and authorization denials. The generated browser admin created and listed data with a screenshot, and remained usable with native physical snapshots. The official MCP SDK created, got, updated, listed, and removed data. An approval-required durable workflow was submitted, its server killed with `SIGKILL`, restarted against the same SQLite database, approved, and observed succeeding; its exported records were exact, replay was idempotent, and a bad token was denied.

Inspection reuses JSON-encoded `Table.snapshot` instead of a duplicate physical-table model and omits uninformative opaque runtime placeholders. Policy validation, snapshotting, freezing, and compilation are centralized in registered construction. Canonical schemas, frozen migration history, authored transactions, and native workflow/entity contracts remain unchanged.

These observations establish the listed composed surfaces and authorization/durability behavior only. They do not establish another database, production identity issuance, multi-runner coordination, or general distributed guarantees.

### 2026-09-09: Declarative compiler compaction

Framework source shrank from 7,189 to 7,057 lines (132 lines and 5,290 bytes removed across four modules), with package exports and public DSLs unchanged. Table checks use a compiler registry that decodes only the selected check payload; numeric literal and enum classification share an implementation. Authorization action metadata now supplies allowed phases and current/candidate requirements from one declaration. SQL expressions use private native `Data` rather than serialization schemas. Migration planning removes repeated intent classification and intermediates, while `plan` and `generate` share artifact construction and canonical serialization. Rebuild copies and incremental steps are still emitted only for the selected path. ([Table](../../packages/effect-domains/src/table.ts); [authorization](../../packages/effect-domains/src/authorization.ts); [SQL policy](../../packages/effect-domains/src/policy-sql.ts); [migrations](../../packages/effect-domains/src/sqlite-migrations.ts))

`bun run check`, `bun run lint`, and all 57 existing tests across 15 files passed. Throwaway programs produced byte-identical output against saved originals: 104 migration-plan combinations, 18 scalar codec/DDL cases, six numeric-boundary matrices, inspection, authenticated CRUD and rollback, cursor pagination, policy short-circuit failures, SQL literal equality, and an executed backfill migration. The existing regressions retain malformed-policy, codec-service, Boolean SQL, relational migration, interacting-rename, and manifest-failure coverage. No permanent tests or new framework abstractions were added. ([Regression suite](../../test/))

A real isolated todo server accepted authenticated CLI creation and nested patch flags, hid the row across tenants, rejected anonymous access and completed-row mutation, retained the last authorized value, and allowed administrator removal. The owned server and temporary source/database artifacts were removed. No browser interaction or durable-execution recovery was revalidated; the comparisons establish exercised behavior, not equivalence for every possible application.

### 2026-09-09: Behavior-preserving implementation reduction

The framework source shrank from 7,329 to 7,189 lines across seven modules, without changing package exports or declared application contracts. Private migration planner state now uses native Effect `Data` rather than serialization schemas; migration artifacts retain their existing schemas. Authorization snapshots reuse `Policy.fold`, SQL conjunction/disjunction share ordered binding, resource mutations share authorization-value construction, cursor SQL loses forwarding helpers, and scalar compilation and CLI flag derivation reuse existing results. ([Migrations](../../packages/effect-domains/src/sqlite-migrations.ts); [authorization](../../packages/effect-domains/src/authorization.ts); [SQL policy](../../packages/effect-domains/src/policy-sql.ts); [resources](../../packages/effect-domains/src/resource.ts); [SQLite](../../packages/effect-domains/src/sqlite-bun.ts); [tables](../../packages/effect-domains/src/table.ts); [CLI](../../packages/effect-domains/src/rpc-cli.ts))

`bun run check`, `bun run lint`, and all 57 tests across 15 files passed. Throwaway programs ran against saved pre-refactor sources and the final implementation: serialized CRUD results, authorization failures, mixed-order cursor pages, inspection, initial/rebuild/blocked migration artifacts, nine scalar codec/DDL cases, and generated patch help matched byte-for-byte. Existing migration regressions also cover repeatable intent flags containing JSON/SQL colons, failed rebuild rollback, relational constraints, and atomic manifest registration. ([Regression suite](../../test/); [migration boundaries](../../test/SqliteMigrations.test.ts))

A real isolated todo server accepted authenticated CLI creation and nested patch flags, hid the row from another tenant, rejected a subsequent completed-row mutation without changing the stored value, and allowed administrator removal. The owned server and temporary sources/databases were removed. No browser surface or durable-execution recovery scenario was revalidated; these observations establish the exercised refactoring boundaries, not equivalence for every possible application.

### 2026-09-09: Command-wrapper removal

The complete cutover deletes `packages/effect-domains/src/commands.ts` and its package export. Reservations and billing join authored SQL in declaring native RPC groups and installing native `toLayer` handlers, registered once through application bundles. The only shared bundle type describes a native group and handler layer; it has no constructor, invocation methods, or context machinery. Explicit error mapping remains outside authored transactions. ([Package exports](../../packages/effect-domains/package.json); [bundle shape](../../packages/effect-domains/src/rpc-contract.ts); [reservation handlers](../../examples/reservations/sqlite.ts); [billing handlers](../../examples/orders-invoices/sqlite.ts))

Live loopback Bun servers and a throwaway Effect/official-SDK client exercised reservation reserve/release, ISO timestamp results, illegal transitions, and a forced insert failure with unchanged stock. Billing completed create/add-line/issue/pay, rejected stale versions, denied reader mutations and anonymous calls, and hid another tenant's order. One MCP session changed from Alice to outsider to anonymous and back to Alice without leaking the earlier identity.

The adapter suites retain Date payload/success/error codecs, handler-service codec contexts, and request-local authentication. A scoped MCP handler regression confirms finalizers run after both success and failure before another call in the same session. Reservation concurrency, rollback, and historical migration tests now invoke native RPC; billing retains its tenant and rollback regressions. Wrapper-only direct-service and optional-mapper type tests were deleted, not recreated as tests of native Effect internals. ([MCP](../../test/RpcMcp.test.ts); [admin](../../test/ApplicationAdmin.test.ts); [reservations](../../test/Reservations.test.ts); [billing](../../test/OrdersInvoices.test.ts))

`bun run check`, `bun run lint`, and `bun run docs:build` passed. All 57 tests across 15 files passed; the count reflects removal of six wrapper-only tests and addition of one MCP invocation-scope regression.

The retired package import fails resolution while `effect-domains/application` still resolves. Owned servers, temporary sources, and isolated databases were removed. No browser UI was changed or visually revalidated. Earlier records below describe the retired command wrapper where named; those historical outcomes are not current API promises.

### 2026-09-09: Native authored-SQL RPC

The authored book application now declares `BooksRpcs` using native `Rpc.make` and `RpcGroup.make`, installs `BooksRpcs.toLayer` handlers, and registers `{ group, handlers }` directly. It removes its `Commands` dependency and separate entrypoint service provision. SQL/schema error translation is explicit in authored Effects; create alone also translates a missing SQL result. Resource table derivation, migrations, runtime adapters, and the other applications' command services are unchanged. ([Contracts](../../examples/expense-ledger/sqlite.ts); [handlers](../../examples/expense-ledger/sqlite.ts); [composition](../../examples/expense-ledger/application.ts); [entrypoint](../../examples/expense-ledger/main.ts))

A throwaway Effect program exercised the real Bun server on loopback with isolated SQLite storage:

- Generated CLI creation/listing/removal, operation inspection, invalid-input rejection, and nonzero missing-row failure.
- Native HTTP `RpcClient` reads, updates, lists, and declared `BookNotFound` after deletion.
- Official-SDK MCP initialization, discovery of all five tools, create/get/update/list/remove, deleted-row results, declared missing-row errors, and session termination.
- SQLite triggers forced both `SqlError` and an absent create result; HTTP RPC and MCP reported `BookPersistenceError` in both cases. The final list was empty.

`bun run check`, `bun run lint`, and `bun run docs:build` passed. The existing suite passed all 62 tests across 16 files. The owned server was stopped, and the throwaway source and isolated database artifacts were removed. This public, JSON-native slice does not establish wrapper equivalence for authentication context, service-dependent codecs, acquisition failure handling, or invocation finalizers. No browser interaction was revalidated.

### 2026-09-09: Tenant-scoped orders and invoices

The slice declares canonical orders, lines, and invoices, exposes generated tenant-scoped reads, and authors create/add-line/issue/pay/summary commands. Resource `relations` supplies composite tenant foreign keys, tenant-local number uniqueness, one invoice per order, and tenant/status indexes. Business rules, authenticated subject binding, checked minor-unit arithmetic, and expected-version writes remain explicit. ([Domain](../../examples/orders-invoices/domain.ts); [resources](../../examples/orders-invoices/resources.ts); [contracts](../../examples/orders-invoices/contracts.ts); [SQL](../../examples/orders-invoices/sqlite.ts); [walkthrough](../../examples/orders-invoices/README.md))

- Workspace/root typechecking and all workspace and root lint checks passed without suppressions. `bun run build` passed. The full suite passed 62 tests across 16 files; the final targeted billing/migration run passed all three tests. ([Workspace commands](../../package.json))
- The authenticated RPC regression covers tenant-local duplicates, hidden reads and writes, reader mutation denial, empty-invoice rejection, totals, stale versions, and legal/illegal payment transitions. A valid foreign-key insert targeting another tenant's existing order fails. A forced invoice-insert failure leaves the order draft at its prior version with no invoice. ([Billing regression](../../test/OrdersInvoices.test.ts))
- Migration regressions upgrade populated unconstrained tables, reject orphaned existing data without losing rows, retry after explicit repair, preserve children while rebuilding their referenced parent, and reject a forged declared index. Frozen pre-relational histories continue to pass the existing migration suite. ([Relational migrations](../../test/RelationalMigrations.test.ts); [existing histories](../../test/SqliteMigrations.test.ts))
- A real loopback Bun server and generated CLI completed order → line → invoice → payment. A reader retrieved the result but could not mutate; another tenant could reuse the same order number but could not read the first tenant's order. Anonymous access failed. Duplicate, stale, and invalid-transition commands exited nonzero without JSON stdout.
- Two concurrent live add-line commands with the same expected version produced exactly one successful write and one `VersionConflict`; the stored summary had one line and version 2. This demonstrates database-guarded concurrency on the exercised single-server SQLite runtime, not distributed conflict resolution.
- Local `inspect` emitted the declared physical relations. The generated browser admin displayed the reader-visible paid invoice, total 2500, and version 2; browser local and session storage remained empty. No custom billing UI was introduced.

This adds a materially different relational business slice, not a production billing system: demo credentials have no identity lifecycle, payment is a local state transition, and taxes, currencies, external providers, retry idempotency, automatic joins, and cross-database transactions remain outside the implementation.

### 2026-09-09: Persistent-reference and cluster-wrapper removal

The user-approved cutover removes `PersistedRef`, its cache-only counter application and dedicated regressions, and the `ApplicationCluster` package export. Caching and concurrency remain application policy. The durable-workflow application now owns the status response schemas and reads native `Sharding` and `RunnerStorage` services directly; its authenticated RPC remains `DurableWorkflow.Status`. Generated CLI flags are unchanged. ([Package exports](../../packages/effect-domains/package.json); [workflow status](../../examples/report-exports/workflow.ts); [remaining SQLite regressions](../../test/SqliteBun.test.ts))

A real workflow server with isolated application/execution SQLite files returned `activeEntities`, `shuttingDown`, and a healthy runner with `host`, `port`, `healthy`, `groups`, and `weight` through the generated authenticated CLI. The same command without credentials exited nonzero with `Unauthenticated`. The owned server was stopped afterward. This verifies diagnostic publication and authentication, not a repeat of workflow recovery testing.

`bun install` removed the retired workspace package. All 62 remaining tests across 16 files passed, and `bun run docs:build` passed. Both retired package subpath imports failed resolution. The workflow application and modified SQLite test passed lint. Aggregate workspace/root typechecking remained blocked by unrelated `sqlite-migrations.ts:860` (`"source"` not assignable to `never`); aggregate lint reported unrelated resource, table, migration, and orders/invoices diagnostics. Those files were not changed by this cutover. ([Workspace commands](../../package.json); [SQLite regressions](../../test/SqliteBun.test.ts))

Earlier counter and persistent-reference observations below describe the retired implementation; they remain historical evidence, not supported current APIs.

### 2026-09-09: Native durable execution

- `bun install`, `bun run check`, `bun run lint`, and `bun run test` passed. The final suite contains 64 tests across 16 files. Native layer requirement checks preserve execution, background, HTTP-request, and RPC-middleware dependencies. ([Workspace commands](../../package.json); [runtime type checks](../../test/Application.types.ts))
- The native WorkflowProxy regression rejects unauthorized submission and resume before entering the engine, and verifies stable execution identity and one execution for repeated submission. Database-isolation regressions reject existing symlink/file-URL aliases, hardlink aliases, and dangling symlinks before opening application storage. The dangling-symlink regression failed before its correction because the worker started and created the shared target. ([Proxy regression](../../test/WorkflowApplication.test.ts); [isolation regressions](../../test/ApplicationExecution.test.ts))
- A real native HTTP route invoking `application.group.accessHandler` returned HTTP 500 before application handler services were preserved in the serving layer. The same route returned HTTP 200 with its handler result after the correction. The throwaway host and source were removed. ([Runtime](../../packages/effect-domains/src/application-bun.ts))
- An approval-required export was accepted through the generated CLI, its process was killed with `SIGKILL`, and the restarted server accepted approval and completed the artifact. Polling reported success and the file contained the submitted JSON. Unauthorized resume failed. Through actual MCP calls, anonymous polling failed with `Unauthenticated`, operator polling succeeded, and repeated export submission returned the original execution ID. ([Workflow](../../examples/report-exports/workflow.ts); [proxy commands](../../examples/report-exports/contracts.ts); [writer](../../examples/report-exports/writer.ts))
- A second export was accepted and its server killed before the five-second durable clock completed. A standalone `worker` reopened the same execution store and produced the expected artifact without an HTTP listener. Workflow status returned a healthy native runner; `/operator/metrics` returned 401 anonymously and 200 with the operator token. Diagnostics have since moved into the application. ([Native layers](../../examples/report-exports/runtime.ts); [metrics](../../examples/report-exports/routes.ts); [current diagnostics](../../examples/report-exports/workflow.ts))
- A reminder delivered through the native Entity proxy returned a receipt; repeating its request ID returned the same receipt and delivery timestamp. A valid editor session was forbidden, and a mismatched entity/payload recipient failed. Another future delivery was accepted, the server was killed with `SIGKILL`, and a standalone worker delivered the persisted request after restart. ([Entity protocol](../../examples/appointment-reminders/appointment-reminder-entity.ts); [authorization](../../packages/example-support/src/subject.ts); [background](../../examples/appointment-reminders/background.ts))
- The reminder worker's native Singleton wrote actual receipt JSON. An explicitly configured five-second ClusterCron archived a fixture older than 90 days while leaving recent receipts unarchived; the normal default remains midnight UTC. Application SQLite contained only its migration ledger and receipt table, while native cluster tables remained in the private execution database. Live probing exposed native Sharding's private-SQL invocation precedence; Entity and Cron registrations now capture application SQL explicitly. ([Background registration](../../examples/appointment-reminders/background.ts); [application history](../../examples/appointment-reminders/migrations/001_initial.json))
- Both standalone workers refused HTTP connections. All owned smoke processes were stopped, and throwaway sources, migration staging, and isolated runtime artifacts were removed. No browser UI was changed or visually revalidated.

These observations establish the demonstrated single-runner restart, authorization, scheduling, and idempotency behavior. They do not establish concurrent multi-runner ownership, production identity, atomic transactions across the two databases, exactly-once external side effects, general event sourcing, or arbitrary workflow cancellation recovery. ([Report runbook](../../examples/report-exports/README.md); [reminder runbook](../../examples/appointment-reminders/README.md); [integration decisions](research-agenda.md#adjacent-native-api-decisions))

### 2026-09-09: Schema and invocation boundaries

- Before concurrent workflow/cluster edits, `bun run check`, `bun run lint`, `bun run test`, and `bun run build` passed, with 61 tests across 14 files. The later expanded suite passed 62 tests across 15 files. Final aggregate type/lint checks reported unfinished concurrent changes, including a new `UnaryRpcProcedure` reference in `commands.ts` and the durable-workflow example. Lint passed for the other modified framework files and every modified boundary test. No rules or suppressions were added. ([Workspace commands](../../package.json))
- Regressions exercise independent request survival after defects; handler-local payload/success/error codecs under conflicting ambient services; direct-call middleware requirements and finalization; and optional mapper outcome typing. ([Admin](../../test/ApplicationAdmin.test.ts); [MCP](../../test/RpcMcp.test.ts); [retired wrapper checks](#2026-09-09-command-wrapper-removal))
- Persistence regressions cover suspended Boolean/timestamp codecs, nullable suspended wrappers, timestamp filtering across cursor pages, rejection of semantic numeric ordering, collision-free patch identifiers, directional subject bindings, nullable-key rejection, and Unicode string validation through SQLite. Rename chains and cycles are checked against applied row values. ([Table](../../test/Table.test.ts); [resources](../../test/ResourceCrud.test.ts); [authorization](../../test/Authorization.test.ts); [migrations](../../test/SqliteMigrations.test.ts))
- A real browser created a Unicode todo with verified ownership, patched it through the generated `{ key, changes }` form, and displayed the changed row. The generated CLI then patched the same row using `--key` and `--changes-title`. The CLI regression also launches a real server and invokes both a no-payload RPC and an empty-struct RPC without input flags. ([CLI regression](../../test/RpcCli.test.ts); [todo application](../../examples/team-tasks/main.ts))
- The document v1 seed was upgraded by normal application startup. Its title survived as `heading`, with `summary: null` and `priority: 0`; the ledger contained migrations 001, 002, and 003. The todo, document, and reservation histories append explicit string-check-removal rebuilds; earlier artifacts are unchanged. ([Document seed](../../examples/editorial-calendar/seed-v1.ts); [document migration](../../examples/editorial-calendar/migrations/003_schema_string_checks.json); [todo migration](../../examples/team-tasks/migrations/003_schema_string_checks.json); [reservation migration](../../examples/reservations/migrations/003_schema_string_checks.json))
- The restarted todo server also accepted and returned a canonical string containing NUL through the generated CLI. Both smoke servers were stopped, the browser tab released, and isolated databases removed.

These checks establish the corrected schema and invocation boundaries, not another database, production identity provider, or materially different business slice.

### 2026-09-09: Repository-wide lint remediation

- `bun run lint`, `bun run check`, `bun run test`, and `bun run build` pass. The test run covers 45 tests across 14 files. No lint rules, exclusions, or suppressions were added. ([Workspace commands](../../package.json))
- Runtime entrypoints executed `ApplicationBun.run` with native `BunRuntime.runMain`; command layers took the optional tagged-error mapper directly. The historical authentication implementation used `AuthorizationRpc.Authenticator`; the current replacement is the [identity service](../../packages/example-support/src/identity.ts). Policy evaluation inputs used schema-backed `AuthorizationValues` and `PolicyEnvironment` with explicit `Option` row/next values. Migration planning continued to accept literal intent arrays. ([Runtime](../../packages/effect-domains/src/application-bun.ts); [retired commands](#2026-09-09-command-wrapper-removal); [policy tests](../../test/Policy.test.ts); [migration tests](../../test/SqliteMigrations.test.ts))
- Policy regressions retain construction snapshots and reject copied or fabricated descriptors. Compiled policies use private instance state rather than a mutable native collection. MCP regressions preserve scalar, array, void, and declared-error responses plus per-call authentication. ([Authorization](../../packages/effect-domains/src/authorization.ts); [authorization tests](../../test/Authorization.test.ts); [MCP tests](../../test/RpcMcp.test.ts))
- Real in-memory Bun servers exercised book create/update/get/remove, native numeric flags, complete JSON inputs, nested todo patch/filter flags, cursor pagination, owner visibility, and anonymous rejection. ([Book CLI guide](../../examples/reading-list/README.md); [todo CLI guide](../../examples/team-tasks/README.md))
- The browser created a book and displayed its stored numeric value. The authenticated todo browser applied an explicit false filter, followed Next, and returned Back to the first page. Initial anonymous loading renders an error, and entering a bearer token then reloading recovers. Local/session storage remained empty. ([Browser client](../../apps/admin/src/client.ts))
- Both smoke servers were stopped, browser tabs released, and temporary formatter tooling removed. Smoke databases were in memory; no throwaway source files were retained.

### 2026-09-09: Abstraction simplification

The approved review cutover removes intermediate runtime schemas and forwarding machinery while retaining canonical/storage/wire separation, narrow runtime services, explicit policy, and frozen migration artifacts. Package source decreased from 6,927 lines across 22 modules to 5,946 lines across 23 modules: 981 fewer lines (14.2%), including the private shared RPC compiler. These are source-line counts, not performance measurements. ([Framework source](../../packages/effect-domains/src/); [maintained contract](tables-and-queries.md))

- `bun run check` passes for all ten workspaces and root integration types. All 45 tests across 14 files pass. `bun run build` passes; the framework's additional `check --noUnusedLocals` check also passed after removing unused schema-derived declarations. ([Workspace scripts](../../package.json); [framework scripts](../../packages/effect-domains/package.json))
- A real in-memory SQLite probe rejected invalid implicit-id creates, updates, and patches for a canonical `lower < upper` check and retained the valid committed row. Table row/storage and repository regressions now defend this previously lost check. ([Table regressions](../../test/Table.test.ts); [repository regressions](../../test/ResourceCrud.test.ts))
- A unary RPC with authentication middleware and no base error declaration now exposes `Unauthenticated` through inspection. Policy regressions cover frozen construction snapshots, malformed syntax, fabricated descriptors, and existing request-local authorization behavior. ([Inspection regression](../../test/ApplicationAdmin.test.ts); [policy regressions](../../test/Authorization.test.ts))
- A nonexistent rename source targeting a new nullable column produced a blocked plan instead of `SqliteAddColumn`. The real todo CLI accepted two backfills with colon-containing JSON strings; replay preserved an existing row and wrote `acme:smoke` and `alice:smoke`. Regressions also cover colon-containing SQL expressions. ([Migration regressions](../../test/SqliteMigrations.test.ts))
- Real public and authenticated Bun servers exercised native numeric flags, JSON fallback, create/update/remove, nested patch/filter flags, cursor output, and anonymous rejection with empty stdout. Native payload records have null prototypes; an isolated CLI regression preserves a `__proto__` field without mutating `Object.prototype`. ([CLI](../../packages/effect-domains/src/rpc-cli.ts); [CLI regressions](../../test/RpcCli.test.ts))
- MCP created an authenticated todo and rejected a subsequent anonymous call on the same session. The browser admin created and patched a todo, displayed the resulting table, denied deletion as the owner, and allowed deletion after switching to the demo administrator. Browser local/session storage remained empty. ([MCP](../../packages/effect-domains/src/rpc-mcp.ts); [admin adapter](../../packages/effect-domains/src/application-admin.ts))
- `bun run lint` still fails. Diagnostics include pre-existing admin issues and rules that demand the removed expression wrappers, prohibit local loops/mutation, or reject optional values. The approved simplification does not imply lint compliance; no lint rules, exclusions, or suppressions were changed.
- Both smoke servers were stopped and the browser tab released. Probe databases were in memory and no throwaway source files were retained.

These checks establish the changed compiler/runtime boundaries, not a new database adapter, production identity lifecycle, or another materially different domain slice.

### 2026-09-09: Bun workspaces and prebuilt admin

The private root now coordinates ten Bun workspaces: the framework and shared example support under `packages/`, plus seven example applications and the browser admin under `apps/`. Public `effect-domains/*` imports retain their names. The admin package owns its presentation contract, browser entrypoint, stylesheet, and build; the native adapter consumes prebuilt assets rather than invoking a browser compiler. ([Workspace](../../package.json); [framework package](../../packages/effect-domains/package.json); [admin package](../../apps/admin/package.json); [build](../../apps/admin/build.ts); [asset loader](../../packages/effect-domains/src/application-bun.ts))

- `bun install` and `bun run build` pass. `bun run check` passes for all ten workspaces and the root integration types; all 40 tests across 14 files pass. The framework type check also passes after making asset-loading failures print their diagnostic.
- Before building, admin-enabled startup failed with the missing asset path and the `bun run build` instruction. CLI help still worked, and an admin-disabled server started with `/admin` returning 404.
- A real browser against the workspace `serve` script created an authenticated todo from a title, listed its server-bound `acme`/`alice` ownership, patched its title, and removed it as the demo administrator. The styled table was visually inspected. Served JavaScript and CSS matched the prebuilt files byte-for-byte; browser token storage remained empty.
- Both distributable packages packed successfully. An isolated consumer outside the repository installed those tarballs, started an application, and returned HTTP 200 for the admin page and both byte-identical assets without the browser entrypoint or build script.
- Lint was not rerun; its previously recorded failure remains unresolved. Workspace lint scripts preserve coverage of the relocated sources, with no new rule suppressions.

These checks establish the workspace, build, distribution, and exercised runtime boundaries, not another domain slice or production authentication.

### 2026-09-09: Declarative inputs and generated admin

The approved cutover adds trusted-subject creation bindings, native Boolean SQL visibility, optional application/runtime configuration, invocation error mapping, and an opt-in generated browser admin. Canonical schemas, operation exposure, authorization decisions, authored transactions, and frozen migration intent remain separate. ([Resource](../../packages/effect-domains/src/resource.ts); [Runtime](../../packages/effect-domains/src/application-bun.ts); [retired commands](#2026-09-09-command-wrapper-removal); [Admin adapter](../../packages/effect-domains/src/application-admin.ts))

- `bun run check` passes. All 40 tests across 14 files pass. Added coverage exercises subject-derived create inputs and forbidden overrides, native Boolean visibility/pagination, mapper error types and finalizer ordering, admin per-call identity isolation, cross-origin/untrusted-origin rejection, Date/void codecs, declared errors, and defect redaction. ([Authorization tests](../../test/Authorization.test.ts); [RPC tests](../../test/AuthorizationRpc.test.ts); [retired command tests](#2026-09-09-command-wrapper-removal); [Admin tests](../../test/ApplicationAdmin.test.ts); [Runtime types](../../test/Application.types.ts))
- Real browser sessions against isolated Bun todo and book applications exercised generated create/list/patch forms, one-item next/back cursor navigation, denied owner mutations, and whole-payload JSON rejection of forged ownership. Todo creation supplied only a title; returned rows contained the verified `acme`/`alice` claims. The final Effect-based patch form read enabled optional fields at submission time.
- Book text containing an HTML image/event-handler string rendered literally in the table: no image element or script execution occurred. A configured `/console` surface applied a custom title, resource/operation labels, description, and column selection. A 390-pixel mobile viewport had no page overflow. Bearer input with credentials present left local and session storage empty. ([Client](../../apps/admin/src/client.ts); [Presentation](../../packages/effect-domains/src/application-admin.ts))
- Generated reservation forms executed reserve and release, returning the authored `held` and `released` states. The CLI and MCP each created a todo from a title alone; a subsequent MCP call without credentials returned `Unauthenticated`. A note created through the CLI returned canonical text while direct SQLite inspection showed `stored:Canonical storage overlay`, exercising the native `Schema.fieldsAssign` storage declaration.
- A minimal `runMain` application without admin returned HTTP 404 at `/admin`. An explicit database filename took precedence over a conflicting environment setting; URL-based manifests initialized the isolated databases. No migration artifacts or canonical schemas were changed.
- `bun run lint` does **not** pass. Remaining diagnostics include DOM/native collection style, declaration spacing, configuration-object rules, and the imperative `runMain` boundary. No rules, exclusions, or suppressions were added; type/runtime verification does not imply lint compliance.
- Final browser filtering selected only the completed row for `true` and only incomplete rows for explicit `false`. All six smoke servers were stopped, browser tabs released, and the three temporary entrypoints and isolated SQLite directory removed.

These checks establish the exercised generated surface, not a production authentication system, inferred permissions, arbitrary custom UI layouts, REST semantics, or another database implementation.

### 2026-09-09: MCP runtime derivation

The `serve` command derived by `ApplicationBun.run` now mounts `/mcp` beside `/rpc/v1`; applications do not redeclare tools. Native Effect MCP handles HTTP/session negotiation, while `RpcMcp` derives tool schemas and dispatches through the existing RPC handlers and middleware. ([Adapter](../../packages/effect-domains/src/rpc-mcp.ts); [Runtime](../../packages/effect-domains/src/application-bun.ts); [Usage](../../examples/README.md#mcp-server))

- `bun run check`, `bun run lint`, and all 36 tests across 13 files pass without suppressions or exclusions.
- HTTP-boundary regressions exercise discovery, Date input/output and tagged-error encoding, array/void results, invalid inputs, defect redaction, and service-dependent wire codecs. A type assertion preserves missing codec services as caller requirements. ([MCP regressions](../../test/RpcMcp.test.ts))
- Concurrent calls sharing one MCP session resolve separate Alice/Bob identities. Anonymous requests before and after authenticated calls, and a server without an authenticator, cannot inherit captured identity. These checks exercise native HTTP requests and RPC middleware rather than direct tool-handler mocks.
- Real Bun example servers with isolated in-memory SQLite exercised initialization, discovery, book create/get/update/list/remove, and missing-row errors. Protected notes rejected anonymous access, allowed editor creation and reader retrieval, denied a reader update without changing the row, and allowed administrator removal. Canonical note text traversed the existing service-dependent storage codec. The unchanged generated RPC CLI still returned the book list.
- Smoke servers were stopped afterward; their SQLite databases were in memory, and no temporary scripts were retained.

This verifies derived HTTP tools, not stdio, inferred MCP resources/prompts, REST, production credential issuance, or external desktop-client interoperability. Tool discovery is public metadata; authorization is enforced on calls.

### 2026-09-09: Authorized examples

The todo and service-codec applications then used authored resource policies and a shared demo-token authenticator; the other five examples were explicitly public. This is a historical fixture description, not the current credential-store implementation. ([Todo policy](../../examples/team-tasks/resources.ts); [Note policy](../../examples/field-notes/resources.ts); [current identity replacement](../../packages/example-support/src/identity.ts); historical `demo-authentication` walkthrough (removed by the later identity cutover))

- `bun run check`, `bun run lint`, and all 34 tests across 12 files pass. The existing codec regression now supplies an authorized subject for its protected note creation. No suppressions or new test scaffolding were added. ([Codec regression](../../test/ResourceCrud.test.ts))
- Real Bun example servers and their generated CLIs rejected missing/unknown credentials; isolated owner and tenant rows; and traversed Alice's three visible todos in one-item cursor pages while excluding interleaved rows. Forged owner/tenant creates and ownership transfers failed. Owners could edit incomplete todos, but not completed ones; denied edits and removals preserved state. An administrator reopened and removed a todo, and the owner could edit again after reopening.
- The note CLI allowed editor create/update and reader reads, denied reader writes and editor deletion, and allowed administrator deletion. Another tenant's editor could read the intentionally global collection. RPC returned canonical `Revised domain text`; direct SQLite inspection showed `stored:Revised domain text`.
- An isolated database was prepared using the original todo migration and seeded with one old-format row. Normal example startup applied `002_ownership`, retained the row, and filled `tenantId = "acme"` and `ownerId = "alice"`. The existing CLI supports one backfill and one transform flag; generation therefore supplied an explicit tenant backfill and constant owner SQL expression. Restart retained both migrated and newly created rows. ([Ownership artifact](../../examples/team-tasks/migrations/002_ownership.json); historical `migrations/manifest.json` (removed by the later JSON/history cutover))
- Both local `inspect` commands exposed subject schemas and rendered action policies without credentials. Both smoke servers were stopped and isolated SQLite files removed afterward.

The session tokens are public fixtures without login, expiry, or revocation. Production identity lifecycle, arbitrary authored SQL authorization, other databases, and distributed coordination remain outside this evidence.

### 2026-09-08: Resource authorization

The user-approved resource policy DSL requires explicit public, deny, or typed authorization. Canonical schemas remain authorization-free. One closed schema-backed AST and fold support evaluation, SQL visibility, reference validation, and inspection; request-local authenticated subjects reach protected generated RPCs. ([Authorization](../../packages/effect-domains/src/authorization.ts); [Policy](../../packages/effect-domains/src/policy.ts); [RPC authentication](../../packages/effect-domains/src/authorization-rpc.ts))

- `bun run check` and `bun run lint` pass without suppressions or exclusions. All 34 tests across 12 files pass. Policy regressions cover SQL/evaluator parity, nulls and hostile values, lazy evaluation, tenant/owner/admin visibility, pagination, denied mutation rollback, unreadable candidates, concurrent ownership changes, unsupported Boolean SQL encoding, and RPC identity isolation. Type checks cover missing authorization, incompatible operand types, and invalid policy phases. ([Policy regressions](../../test/Policy.test.ts); [authorization regressions](../../test/Authorization.test.ts); [RPC regressions](../../test/AuthorizationRpc.test.ts); [type boundaries](../../test/Authorization.types.ts))
- A real Bun HTTP server and generated CLI used isolated in-memory SQLite and an authored test-session authenticator. Missing credentials yielded `Unauthenticated`; another owner's row and another tenant's row yielded `ResourceNotFound`. Two one-item cursor pages returned only the caller's documents.
- Live CLI mutations rejected ownership transfer without changing the original owner, accepted an allowed title patch and owned create/remove, and reported the removed row missing. A forged-owner create failed with `Forbidden` and left no row. The smoke server was stopped; temporary helpers were removed.

This establishes the exercised SQLite resource and request-identity boundary, not production credential issuance/revocation, authorization of arbitrary authored SQL, another database, or another business-policy domain. ([Resource](../../packages/effect-domains/src/resource.ts); [SQLite adapter](../../packages/effect-domains/src/sqlite-bun.ts); [Bun runner](../../packages/effect-domains/src/application-bun.ts))

### 2026-09-08: CRUD and JSON codec simplification

The user-approved change makes basic book CRUD a generated resource declaration and moves custom book contracts and SQL to `authored-sql`. Both use the same canonical `BookSchema`; the authored example retains its custom errors and deleted-row remove result. `Commands.rpc(tag, { payload, success, error })` derives JSON codecs and returns a native RPC, replacing wire-schema declarations in authored books, counters, and reservations without a second contract format. ([Basic resource](../../examples/reading-list/resources.ts); [Authored contracts](../../examples/expense-ledger/sqlite.ts); [retired commands](#2026-09-09-command-wrapper-removal))

- `bun run check` and `bun run lint` pass without suppressions. All 25 tests across nine files pass. The added regression exercises Date payload/success and tagged-error JSON codecs. ([Retired command regressions](#2026-09-09-command-wrapper-removal); [Tests](../../test/))
- Real Bun HTTP servers with isolated in-memory SQLite databases exercised both book applications through their generated CLIs: create, get, list, update, remove, and a missing-row failure. Basic CRUD generated UUIDv7 identifiers, returned `void` on removal, and reported `ResourceNotFound`; authored SQL returned the deleted row and reported `BookNotFound`. Both local `inspect books.create` commands selected the expected contract without connecting to a server. ([Basic entrypoint](../../examples/reading-list/main.ts); [Authored entrypoint](../../examples/expense-ledger/main.ts))
- The migrated counter RPCs exercised get, increment, set, a stale cached get, and refresh. Reservation RPCs exercised reserve, release, confirm, `InvalidReservationState`, and stock accounting; timestamp values remained ISO strings over JSON. All smoke servers were stopped, and no smoke files or databases were retained. The counter application has since been retired. ([Reservation entrypoint](../../examples/reservations/main.ts))

These checks cover the listed unary JSON contracts. They do not establish streaming support, authenticated middleware, another database, or another business-policy domain.

### 2026-09-08: Native RPC contract verification

The preceding user-approved cutover replaced custom input/output/error records with native `Rpc.make` and `RpcGroup.make` declarations. `Commands.make({ name, group })` retained the supplied group and injectable unary service behavior. JSON codec derivation was explicit at this stage; the later simplification above moves that work into `Commands.rpc`. Links point to the current authored-book location; the counter application has since been retired. ([Retired commands](#2026-09-09-command-wrapper-removal); [Book RPCs](../../examples/expense-ledger/sqlite.ts); [Reservation RPCs](../../examples/reservations/sqlite.ts))

- `bun run check` and `bun run lint` pass without suppressions. All 24 tests in nine files pass, including captured handler dependencies and invocation-context precedence. ([Retired command regression](#2026-09-09-command-wrapper-removal); [Tests](../../test/))
- Real Bun HTTP servers with isolated in-memory SQLite databases exercised all three migrated applications through their generated CLIs: book create/get/list/update/remove and `BookNotFound`; counter get/increment/set, a stale cached read, and refresh; reservation reserve/release/confirm, `InvalidReservationState`, and stock accounting. Reservation timestamps remained ISO strings over JSON. Local `inspect books.create` selected the native RPC contract. The servers were stopped afterward; no smoke files or databases were retained. The authored book entrypoint has since moved from basic CRUD to authored SQL, and the counter application has been retired. ([Book entrypoint](../../examples/expense-ledger/main.ts); [Reservation entrypoint](../../examples/reservations/main.ts))

These checks establish the migrated unary contracts, not streaming RPC support, authentication middleware, or additional transports.

### 2026-09-08: Lint remediation verification

- `bun run lint` passes with no diagnostics; no exclusions or rule suppressions were added. `bun run check` passes, and all 24 tests in nine files pass. The cutover uses explicit construction records and runtime options, Effect-native operations, and schema-validated dynamic boundaries. Callers and documentation use the updated contracts. ([Lint configuration](../../better-typescript.json); [Application](../../packages/effect-domains/src/application.ts); [Tests](../../test/))
- Isolated Bun HTTP servers exercised generated todo creation with defaults, nested boolean patch flags, filtered pagination, and removal. Local inspection emitted the selected schema and rejected an unknown operation. A service-dependent note creation returned canonical text while direct SQLite inspection showed `stored:Canonical smoke`. ([Todo entrypoint](../../examples/team-tasks/main.ts); [Note codec](../../examples/field-notes/storage.ts); [CLI](../../packages/effect-domains/src/rpc-cli.ts))
- A disposable SQLite scenario exercised the now-retired `PersistedRef.fromResource` with an already-decoded numeric codec input, an omitted default, an omitted generated value, bound-key persistence, and reuse of an existing row. The derived `createInputSchema` validated canonical creation input without reapplying codecs or requiring a complete row. The smoke script and databases were removed afterward. ([Resource](../../packages/effect-domains/src/resource.ts))

These checks cover the listed paths; the broader six-application observations below remain historical evidence.

### 2026-09-08: Simplification verification

The user-approved cutover requires initial migration history, removes the framework Query wrapper in favor of Effect primitives, and preserves generated native CLI flags. Source size fell from 5,428 to 4,128 lines; the generic AST algebra, direct table-write service, database alias, opaque descriptor schema classes, and migration bootstrap/adoption state were removed. ([Table](../../packages/effect-domains/src/table.ts); [Runtime](../../packages/effect-domains/src/sqlite-bun.ts); [Migrations](../../packages/effect-domains/src/sqlite-migrations.ts); [Authored SQL, current location](../../examples/expense-ledger/sqlite.ts))

- All six applications started as real Bun HTTP servers against isolated SQLite files. Generated CLIs exercised authored book create/get/update/remove and missing-row errors; todo defaults, two cursor pages, nested patch flags, mixed-input rejection, and invalid-patch preservation; canonical note create/update; counter increment, cached reads, direct writes and refresh; and reservation hold/release stock accounting. Physical note storage contained `stored:revised` while RPC returned `revised`. ([Example guide](../../examples/README.md))
- The historical document seed upgraded to `heading`, retained its identifier, and gained `summary: null` and `priority: 0`. Restart retained the same row; the database contained two ledger entries and no separate schema-state table. Local `inspect todos.patch` emitted the selected operation. ([Document seed](../../examples/editorial-calendar/seed-v1.ts); [Inspection](../../packages/effect-domains/src/application-inspect.ts))
- A disposable command-only application exercised native string enums, numeric literals, boolean literals, finite positive/negative numeric flags, and present/absent optional nested fields. Invalid native enum values failed through Effect CLI's parser, which prints usage on stdout and the error on stderr; schema/business failures exercised above kept stdout empty. ([CLI interpreter](../../packages/effect-domains/src/rpc-cli.ts))
- `bun run check` passed. All 24 tests in nine files passed, including migration rollback/registration/drift, missing initial history, cyclic scalar rejection, reservation transitions, and codec-service requirements. Obsolete Query/algebra tests and tests of wrapper metadata or void return plumbing were removed rather than repinned. ([Tests](../../test/))
- `bun run lint` did not pass. Its diagnostics include nested-call, declaration-spacing, optional-property, collection-style, and direct-control-flow restrictions; no rules were disabled. Passing tests and type checking do not imply lint compliance.

This verifies only the listed scenarios, not another database adapter or business domain.

### Earlier 2026-09-08 verification (pre-simplification)

The following was exercised against the preceding descriptor, resource, manifest, and single-runner APIs:

- All six applications ran through their actual CLI/server surfaces. Todo creation defaulted `completed` to `false`; patching nested resource input succeeded; generated identifier override was rejected; filtered listing selected completed rows; separate cursor traversal covered four rows, including equal titles, without duplicate identifiers; and restart retained data. A deterministic runtime `Value` service supplied an explicit generated identifier and timestamp, while caller overrides were rejected. ([Todo resource](../../examples/team-tasks/resources.ts); [Todo entrypoint](../../examples/team-tasks/main.ts); [Value](../../packages/effect-domains/src/value.ts); [resource regressions](../../test/ResourceCrud.test.ts))
- The notes application returned canonical plain text over RPC while SQLite stored its `stored:` form. The now-retired persisted counter accepted eight simultaneous increments, retained process-local cache after a direct write, then loaded the direct value through refresh and restart. This exercised local synchronization only. ([Note storage](../../examples/field-notes/storage.ts))
- Reservation reserve/release succeeded; confirming a released reservation returned `InvalidReservationState` with nonzero stderr. The authored book application created, read, and listed rows through SQL. ([Reservation policy](../../examples/reservations/sqlite.ts); [Book SQL, current location](../../examples/expense-ledger/sqlite.ts))
- The historical document application preserved existing identifier and title while upgrading it to `heading`, retained nullable `summary`, and backfilled `priority: 0`. A real migration runner generated an initial manifest artifact, preserved an existing row, rejected a required `priority` change with its exact blocked reason and no registry change, then generated `--backfill documents:priority:7`; the next server loaded that manifest and preserved the original identifier/title with priority `7`. The ledger contained `001_initial` and `002_priority`. ([Document entrypoint](../../examples/editorial-calendar/main.ts); [migration fixture](../../examples/editorial-calendar/migrations.ts); [migration regressions](../../test/SqliteMigrations.test.ts))
- `inspect todos.create` worked with an invalid remote URL because inspection is local; an unknown operation failed. Inspection includes generated schemas and resource/storage policy rather than a generated interface. ([Inspection](../../packages/effect-domains/src/application-inspect.ts); [Bun runtime](../../packages/effect-domains/src/application-bun.ts))
- `bun run check` passed, and all 33 tests in 11 test files passed. An earlier `better-typescript@0.4.12` lint attempt crashed with `SIGSEGV` in `inflight_dedupe_map.markGetOrStart`; the final attempt completed with exit code 1 and 772 diagnostics across 30 files. Leading categories were nested-call restrictions, declaration spacing, Effect collection conventions, and optional-property restrictions. Lint is not counted as passing verification; no rules were disabled.

This is evidence for the listed SQLite/application paths. It does not validate another database adapter or a second materially different business-policy slice.

### Historical verification (pre-2026-09-08 API)

Earlier evidence recorded a fresh-database scalar-field propagation experiment, 28 passing repository tests, five supporting persistent applications, and ten concurrent counter increments. It covered older command-record/application composition and historical example entrypoints; retain it as historical context, not a claim about the current native RPC bundles, manifest loading, or `ApplicationBun.run` contract.

That historical work observed generated todo CRUD, authored book queries, service-dependent note codecs, persisted counter refresh, and historical document rename/backfill. It also exercised reservation no-oversell, rollback, terminal transitions, and timestamp replay. The linked sources remain useful for the underlying behavior: [reservation regressions](../../test/Reservations.test.ts), [table regressions](../../test/Table.test.ts), [SQLite migration regressions](../../test/SqliteMigrations.test.ts), and [example guide](../../examples/README.md).

## Evidence Boundary

The current protected examples begin signed out and use the bounded credential-store identity slice; public examples remain public. These loopback demonstrations are not production deployment templates. Evidence now includes issued, expiring, revocable credentials with current-account checks, bounded workflow/entity restart recovery, and tenant-scoped relational billing with stale-write rejection; it does not establish a production IdP/user-management lifecycle, concurrent multi-runner coordination, idempotent reservation or billing creation, arbitrary cancellation recovery, another database, or broad business-domain fit. Applications own cache invalidation and concurrency policy. ([Current identity/browser cutover](#2026-09-11-native-identity-and-browser-client-cutover); [Billing verification](#2026-09-09-tenant-scoped-orders-and-invoices); [Durable verification](#2026-09-09-native-durable-execution))

Further architectural evidence should add richer domain slices and explicit distributed failure scenarios, not generalize a jobs or actor framework from these bounded native compositions.
